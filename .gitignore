# OS / editor noise
.DS_Store
Thumbs.db
.idea/
.vscode/

# vendored deps
node_modules/

# build outputs
bin/
dist/
build/
out/
*.test
tscheck/.fresh-emit/

# Python (consult/audit helper scripts)
__pycache__/
*.py[cod]

# credentials — env-provided only, never commit
.env
.env.*

# test / coverage artifacts
coverage/
*.coverprofile
playwright-report/
test-results/

# linked git worktrees nested under .agents (see git worktree list)
/.agents/worktrees/
