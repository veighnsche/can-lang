package app
    provides []
    uses [http]
fn http::server_response health
    emits []
    given
        http::request req
    asserts
        sample: => ok
    ok call http::response_text(call http::status_ok(), call http::empty_server_headers(), "healthy")
fn http::router table
    emits [http::invalid_route, http::duplicate_route, http::ambiguous_route]
    asserts
        sample: => ok
    match chain
        call http::route_get("/health", callable health) as http::route probe
        call http::make_router([probe]) as http::router built
        ok => ok built
        http::invalid_route
        http::duplicate_route
        http::ambiguous_route
fn bool settings
    emits []
    asserts
        sample: => ok true
    match call http::make_server_config("127.0.0.1", 0, 0, 1000)
        ok http::server_config config => ok false
        http::invalid_server_config => ok true
fn bool bounds
    emits [http::invalid_server_config]
    asserts
        sample: => ok true
    match chain
        call http::make_server_config("127.0.0.1", 0, 67108864, 2147483647) as http::server_config config
        ok => ok true
        http::invalid_server_config
fn http::server boot
    emits [http::invalid_server_config, http::invalid_route, http::duplicate_route, http::ambiguous_route, http::bind_failed]
    given
        int port
    asserts
        sample: 0 => ok
    match call http::make_server_config("127.0.0.1", port, 65536, 5000)
        ok http::server_config config => match call table()
            ok http::router built => match call http::server_start(config, built)
                when
                    sample: config, built => ok
                ok http::server sturdy => ok sturdy
                http::bind_failed
            http::invalid_route
            http::duplicate_route
            http::ambiguous_route
        http::invalid_server_config
fn void serve
    emits [http::invalid_server_config, http::invalid_route, http::duplicate_route, http::ambiguous_route, http::bind_failed, http::shutdown_failed]
    given
        int port
    asserts
        sample: 0 => ok
    match call http::make_server_config("127.0.0.1", port, 65536, 5000)
        ok http::server_config config => match call table()
            ok http::router built => match call http::server_start(config, built)
                when
                    sample: config, built => ok
                ok http::server sturdy => match call http::server_stop(sturdy)
                    when
                        sample: sturdy => ok
                    ok => match call http::server_wait(sturdy)
                        when
                            sample: sturdy => ok
                        ok => ok
                        http::shutdown_failed
                    http::shutdown_failed
                http::bind_failed
            http::invalid_route
            http::duplicate_route
            http::ambiguous_route
        http::invalid_server_config
fn void main
    emits [http::invalid_route, http::duplicate_route, http::ambiguous_route]
    given
        str[] args
    asserts
        sample: [] => ok
    match call table()
        ok http::router built => ok
        http::invalid_route
        http::duplicate_route
        http::ambiguous_route
