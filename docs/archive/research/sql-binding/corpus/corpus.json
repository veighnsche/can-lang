{"cases": [
  {"id": "select-params", "notes": "P13 descriptor shape: filter param plus top-level LIMIT param", "sql": "SELECT id, display_name FROM accounts WHERE display_name ILIKE $1 ORDER BY id LIMIT $2"},
  {"id": "repeated-param", "notes": "same parameter token twice plus a second parameter", "sql": "SELECT $1, $1 + $2"},
  {"id": "param-high-number", "notes": "multi-digit parameter number", "sql": "SELECT $16"},
  {"id": "string-param-like", "notes": "dollar digits inside a single-quoted string are not parameters", "sql": "SELECT 'it''s $1 not a param'"},
  {"id": "escape-string", "notes": "escape-string constant keeps scanning past backslash quote", "sql": "SELECT E'esc \\' $1', $2"},
  {"id": "dollar-quote", "notes": "anonymous and tagged dollar-quoted bodies hide dollar digits", "sql": "SELECT $$body $1$$, $tag$inner $2$tag$, $3"},
  {"id": "line-comment", "notes": "line comment hides a parameter on its own line", "sql": "-- trailing $1 comment\nSELECT $2"},
  {"id": "nested-block-comment", "notes": "PostgreSQL nests block comments", "sql": "/* outer /* inner $1 */ still comment $2 */ SELECT $3"},
  {"id": "unicode-comment", "notes": "multibyte comment bytes precede the parameter token", "sql": "-- caf\u00e9 \u2603 snowman\nSELECT $1"},
  {"id": "unicode-ident", "notes": "quoted unicode identifier before the parameter token", "sql": "SELECT \"caf\u00e9\" FROM t WHERE x = $1"},
  {"id": "malformed", "notes": "grammar error must carry message plus cursor position", "sql": "SELECT FROM WHERE"},
  {"id": "unterminated-string", "notes": "scanner error must carry message plus cursor position", "sql": "SELECT 'oops"},
  {"id": "multi-statement", "notes": "two statements without trailing semicolon", "sql": "SELECT $1; SELECT 2"},
  {"id": "multi-statement-semi", "notes": "two statements with trailing semicolon", "sql": "SELECT 1; SELECT $1;"},
  {"id": "empty", "notes": "empty input parses to no statements", "sql": ""},
  {"id": "comment-only", "notes": "comment-only input parses to no statements", "sql": "-- nothing here\n"},
  {"id": "semicolon-only", "notes": "lone semicolon parses to no statements", "sql": ";"},
  {"id": "insert-values", "notes": "admitted execute shape", "sql": "INSERT INTO t (a, b) VALUES ($1, $2)"},
  {"id": "insert-returning", "notes": "RETURNING must be visible to the I37 shape check", "sql": "INSERT INTO t (a) VALUES ($1) RETURNING id"},
  {"id": "update-where", "notes": "admitted execute shape", "sql": "UPDATE t SET a = $1 WHERE id = $2"},
  {"id": "delete-where", "notes": "admitted execute shape", "sql": "DELETE FROM t WHERE id = $1"},
  {"id": "limit-subquery", "notes": "inner LIMIT param plus top-level LIMIT param", "sql": "SELECT * FROM (SELECT id FROM t LIMIT $1) s LIMIT $2"},
  {"id": "limit-literal", "notes": "literal limit is not a parameter", "sql": "SELECT id FROM t LIMIT 10"},
  {"id": "select-no-params", "notes": "no parameters at all", "sql": "SELECT 1"}
]}
