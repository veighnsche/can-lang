# Audit before dispatch

Three independently rewritten packets preserve: thirteen-capability ASAP scope; no active-implementer changes; all six Can identity/native constraints; no compatibility or external npm feature dependencies; pinned runtime; both SQLite outcomes and exact direct adapter; PostgreSQL front end and async ownership; missing MySQL/transaction qualification; three lifecycle comparison cases and existing owner; absent comparative programs; all four parser observations, prefix behavior, precision/duplicate limits; unsafe native HTML and available callbacks/builder. Four independent questions have identical alternatives and qualification conditions. Every context, instruction and option description differs across packets, checked mechanically; identifiers and measured numbers intentionally remain stable. Manual semantic comparison found no changed premises. Options express real alternatives, including rejected contracts; probabilities are advisory and partly reflect supplied constraints. The native probe tests only samples, not exhaustive conformance.
