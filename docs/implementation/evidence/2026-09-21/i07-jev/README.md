# I07 sequencing consultations

Three fresh requests rewrote every explanatory state, instruction and option
while preserving C5 evaluation order, bool short circuit, single-evaluation
comparison chains, C6 native equality choices, I07/I10 boundaries, and the same
three implementation alternatives. Mechanical comparisons checked all prose slots
before dispatch. Raw requests and responses are retained.

All three jev-1.13.0 responses selected ordered statement blocks, with reported
confidence 1.0. Agreement was treated as advice, not proof or guaranteed bias
removal. Native generated-program tests independently check exact event order,
skipped calls/faults and an awaited boxed-call insertion point.
