# I08 callable construction decision

Three fresh requests preserve the same facts and alternatives while rewriting
all explanatory prose: once-only receiver and exact-name near captures,
interleaved declaration slots, exact residual signature, error subsets, native
function identity, protected completion carriers, and invocation-time assertion
context. All three compare a fresh native closure, bind alone, and invocation-time
name lookup. The native declaration parser remains I16 work in every request.
The saved requests were checked for semantic equivalence and wording differences.

All three responses selected `closure` with confidence 1.0. No disagreement
required investigation. This is advisory evidence; executable tests establish
capture order, identity, context forwarding and failure behavior. Bind alone
cannot fill interleaved slots and the private trailing context argument; delayed
lookup violates creation-time capture. Native closures retain immutable aliases
without adding a Can interpreter or copying values.
