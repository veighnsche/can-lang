# I09 import-validation correction consultations

Three fresh requests retained the same facts, invariant, alternatives and
integration obligations while rewriting all context, questions and option
prose. Pairwise wording checks ran before submission; semantic equivalence was
reviewed separately. Raw requests and responses are retained here.

All three `jev-1.13.0` results chose `parser`, with reported confidence 0.91,
0.73 and 0.97. There was no choice disagreement. The second assigns more weight
to construction provenance; that alternative would require migrating every
string producer before it could establish the invariant. An upstream AST check
repairs the current interface directly. Agreement is advice, not proof or a
claim that prompt bias has been eliminated.

Live TypeSafe index, HTTP API and Choice documentation were read via HTTPS.
Executable parser regressions and the packaged offline test provide independent
evidence. No authored-Can exploit or general hostile-JavaScript sandbox claim
is inferred from the internal computed-import probe.
