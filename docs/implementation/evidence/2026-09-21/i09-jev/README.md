# I09 publication-foundation consultations

Three fresh requests compare directory-confined operations with OS writer/run
locks and exact ownership inventories, ordinary path checks with PID lock files,
and wholesale output replacement. Each request rewrites all explanatory state,
instructions and option descriptions while retaining the same platform evidence,
publication/cleanup requirements, alternatives, implementation obligations and
absence of compatibility/performance claims. Pairwise wording differences were
checked before sending; semantic equivalence was reviewed separately.

Raw requests and responses are retained as `request-{1,2,3}.json` and
`response-{1,2,3}.json`. All three responses identify `jev-1.13.0` and choose
`confined` with reported confidence 1.0, 0.99 and 0.99. No disagreement needs
resolution. Agreement remains advice, not proof or guaranteed wording-bias removal.

Executable evidence independently verifies directory locking across processes,
interruption recovery, conservative ownership checks, native ESM publication and
a child-held lease that survives closing the launcher's descriptor.
