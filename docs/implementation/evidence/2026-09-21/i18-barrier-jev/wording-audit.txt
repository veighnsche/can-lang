Pre-dispatch semantic audit: all three requests preserve full lexical ordering, explicit reserved/running quiescence, one-at-a-time native delivery, incremental earlier arrivals, unique identities, fixed canonical order and adversarial comparator behavior, reject-all/reactivation recovery, measured counts, existing empty-selection fix, native-operation policy and absent host priority queue, required complexity, and the same three alternatives. Every context, question and option prose string differs pairwise. Technical identifiers and mathematical bounds retain their meaning. Agreement is advice, not proof.
