# I19 contextual aggregate inference

Three fresh requests, each with rewritten state, instructions and option descriptions, chose `contextual_hook` with probability/confidence 1.0. The requests preserve identical facts, alternatives and exclusions; see `wording-audit.txt`. No disagreement occurred. These results are advice, not proof of the implementation.

The implementation records exact named-variant constraints at the `all_failed.failures` projection. Forwarding resolves the existing enclosing error-bound specialization. For observations, discovery can defer a contextless initializer while retaining only its authored binding type; a later typed argument supplies the unique variant. Discovery IR is discarded, then the complete handler is checked again using sealed evidence. Missing or conflicting variants are finite errors. Same-scope expression discovery also reaches later named-call constraints when an earlier operand is deferred. Provisional evidence is discarded; the final full check still rejects invalid operands. Nested aggregate aliases stop at their handler scope. No variant search, guessed source union, array covariance or inferred error bound is introduced.

Coverage requires every declared participant error and `standard_failure` in the named variant. The runtime constructs one typed aggregate from original nominal domain payloads and opaque standard snapshots in participant order. Local fallback without observing the payload continues to need no source aggregate specialization.

Checker regressions cover contextual observation, an earlier contextless length read, contradictory named variants, missing context, forwarding bounds, and missing coverage leaves. Offline staged execution and strict generated TypeScript checks cover both forwarding and observed payloads. Runtime tests preserve generic domain payload identities, duplicate occurrences, and the original opaque standard cause. The final acceptance evidence is recorded in [I19 validation](../i19-validation.md).
