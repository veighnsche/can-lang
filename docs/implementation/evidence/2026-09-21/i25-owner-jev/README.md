# I25 opaque map ownership consultation

Three fresh requests compared private immutable containment evidence consumed by
existing participant preparation, creation-time leases, and reflective discovery
through opaque representations. The pre-dispatch audit checked identical facts,
constraints and alternatives with all explanatory wording rewritten. Requests and
raw responses are retained. The previously read TypeSafe HTTP/Choice documentation
supplied the API contract.

All three responses selected `private_evidence` with probability/confidence 1.0
(jev-1.13.0). No disagreement arose. This advice did not establish correctness:
baseline regression reproduction, runtime lease behavior and compiler capture
classification are checked separately in the correction evidence.
