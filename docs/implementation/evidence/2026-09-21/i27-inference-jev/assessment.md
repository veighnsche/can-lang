# Generated-spread shape inference consultation

Three fresh Jev requests selected `shared_private`; reported confidence was 1,
1 and 0.97. There was no alternative selection to investigate. All requests,
responses and the pre-dispatch semantic/wording audit are retained here.

The recommendation is advisory. The correction independently preserves the
public solver's sealed-evidence requirement, reuses its transactional equality
walk, and restricts pre-seal evidence to complete interned nodes in the current
builder graph. The normal checker still validates expressions and arm contracts.
No candidate instantiation search, numeric conversion or compatibility shortcut
is introduced. Baseline and staged regressions provide the implementation proof.
