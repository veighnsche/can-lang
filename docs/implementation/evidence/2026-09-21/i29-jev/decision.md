All three fresh consultations selected `bounded` with probability 1.0. Their
agreement is advice, not proof. The implementation follows P8's caller budget
without an undocumented fixed cap, validates negatives before input, and retains
native allocation defects in the standard channel. Tests establish the behavior.
Empty environment values are preserved as present entries; bearer credential
rejection remains a separate transport policy. Live TypeSafe API/Choice docs were
read before the calls; requests, responses and pre-dispatch audit are retained.
