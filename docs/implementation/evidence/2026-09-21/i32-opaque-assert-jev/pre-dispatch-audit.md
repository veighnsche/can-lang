# I32 opaque-assertion consultations: pre-dispatch audit

All three requests ask the same single question over the same facts,
constraints, and alternatives. Exact identifiers are preserved verbatim:
`http::server_response`, `emits []`, outcome mismatch, C/P10/P13 section
roles, `supplied-completion`.

Facts held constant: fixed handler contract; mandatory assertions with
measured always-mismatch rows; fresh-token constructors with
identity-plus-deep-equality comparison; C exclusion of opaque values; P10
write-only sinks without readers; fixture-path verification of boundary
calls and arguments; staged loopback wire verification; HTML precedent of
asserting failures and void.

Wording differences (intentionally rewritten, semantically equivalent):
- Request 1 frames the failure as "staged runs prove" and asks for "honest
  passing assertions without breaking response opacity".
- Request 2 frames tokens as "new token" versus "identity with deep
  equality" and asks "how should passing handler assertions be shaped".
- Request 3 frames the contract as "one rigid shape" and asks for "the
  assertion design that stays truthful".

No request adds, drops, or narrows an option: all four offer
bare_opaque_ok / exempt_handlers / vacuous_runner /
structural_response_compare with matching definitions in identical order.
