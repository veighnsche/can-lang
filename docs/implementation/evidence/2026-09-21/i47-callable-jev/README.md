# Structural type bridge consultation

Three fresh requests compared reuse of I04's concrete type parser plus a
structural bridge, duplicate callable/arm grammar in the inventory parser, and
bypassing structural parsing with an admission hook. All explanatory fields
(context, instructions, and all option descriptions) were rewritten. The script
checked pairwise prose inequality; manual review checked equivalent facts,
constraints and alternatives before sending. Exact technical identifiers remain.

All responses selected `bridge`. The reported probabilities were 1.0, 0.96,
and 0.98; confidence was 0.99, 0.94, and 0.97. All returned `jev-1.13.0`.
No selection disagreement required investigation. Requests/responses are saved
verbatim. Agreement is advice, not proof or a guarantee of bias removal; the
regression and specialization tests establish the implemented behavior.
