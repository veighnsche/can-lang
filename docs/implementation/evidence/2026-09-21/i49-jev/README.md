# I49 occurrence-identity consultations

Three fresh Jev requests compare the same three alternatives: allocate an opaque
occurrence at each origin and preserve it when forwarded; cache by native cause;
or mint at every boundary. All explanatory prose in the state, question
instructions and option descriptions was rewritten for each request. Technical
identifiers and the alternatives remain equivalent. The supplied facts distinguish
independent throws of the same object, primitive throws, propagation, private
cause/origin metadata, and the existing primitive fault brand. No request claims
performance evidence or asks the classifier to research.

Raw [request 1](request-1.json), [request 2](request-2.json), and
[request 3](request-3.json), with the corresponding
[response 1](response-1.json), [response 2](response-2.json), and
[response 3](response-3.json), are preserved without credentials.

All three responses identify `jev-1.13.0` and choose `tokens` with reported
confidence 1.0. There is no disagreement to resolve. Agreement is advisory, not
proof of correctness or independence from wording bias. Native tests separately
verify that independent origins differ, propagation preserves the token, and
public observations never reveal private cause/origin metadata.
