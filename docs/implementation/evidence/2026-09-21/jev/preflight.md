# Preflight, before live calls

The coordinator manually compared all three complete requests, including the shared context, each topic's facts, every question and every candidate description. This is a single-reviewer semantic check, not an independent-agent review. The mechanical check verifies different corresponding prose; it cannot prove semantic equivalence.

Preserved facts in all three:

- No external compatibility requirement; Go compiler, immutable functional Can, TS/Bun target, native reuse, adapter justification, macOS arm64 first, advice-only classifier without research ability.
- Compiler: regex/string-kind AST, revision/proof/Go evaluator coupling, decimal/flattened/synchronous emission; new nominal/region/AI/async/assertion requirements; limited reuse candidates; interface cost, runnable slices, in-place conversion risk, old-IR bridge tradeoff, no measured schedule.
- Packaging: no separate Bun install but no one-file requirement; unchanged private versioned sidecar, lookup/integrity/update work and no startup write; embedding requires executable extraction, version/digest cache, permissions, atomic publication, concurrency, link protection, cleanup, duplicate disk footprint and writable executable cache; both need architecture/notices/integrity/trust testing; installed signed Bun1.4.2 observation only; Can container/extraction untested; no in-process Bun, fork or PATH fallback.
- SQL: actual libpg_query parser/scanner; Bun.SQL runtime ownership; official CGo build-only C requirement; WASM/wazero same parser with extra engine and wrapper risk; upstream tests are not exhaustive; no local size/startup/scanner parity measurements; exact binding/PG major pins; no custom SQL parser.
- Compiler alternatives: fresh typed pipeline/selective reuse/working slices/retirement; direct in-place transformation; temporary old-IR bridge; insufficient evidence.
- Packaging alternatives: versioned sidecar; embedded atomic verified extraction; insufficient evidence.
- SQL alternatives: CGo-first comparative spike and gated adoption; WASM-first comparative spike and identical gates; neutral measurement before preference. None authorizes immediate dependency adoption.

No request includes earlier answers, the engineer's preferences, model-created facts or a favored-option instruction. Technical identifiers and option keys intentionally remain stable. All explanatory prose has been rewritten. Comparing differently phrased prompts does not guarantee removal of bias.

Engineering preference recorded before results: fresh typed pipeline; sidecar; CGo-first comparative spike, with no SQL binding admission until measured. These are justified in stage2 independently of Jev.
