import { types } from 'node:util';
const out:any={bun:Bun.version,revision:Bun.revision,platform:process.platform,arch:process.arch};
out.features={rawJSON:typeof JSON.rawJSON,fromAsync:typeof Array.fromAsync,toSorted:typeof [].toSorted,intersection:typeof Set.prototype.intersection,isProxy:typeof types.isProxy};
out.intersection=[...new Set([3,2,1]).intersection(new Set([1,2]))];
const tokens:any[]=[]; const j=JSON.parse('{"large":9007199254740993,"z":-0}',(k,v,c)=>{if(k)tokens.push([k,c.source]);return v});out.json={tokens,raw:JSON.stringify({large:JSON.rawJSON('9007199254740993')}),negativeZero:Object.is(j.z,-0)};
let called=0;const payload={then(){called++}};const box=Object.assign(Object.create(null),{value:payload});out.boxIdentity=(await Promise.resolve(box)).value===payload;out.thenCalls=called;
const events:number[]=[];out.fromAsync=await Array.fromAsync([0,1,2],async i=>{events.push(i);return Object.assign(Object.create(null),{value:i})});out.mapEvents=events;
out.deepEqual={nan:Bun.deepEquals({x:NaN},{x:NaN},true),zero:Bun.deepEquals({x:0},{x:-0},true)};
out.text={codeUnits:'😀'.length,scalars:[...'😀'].length,literalReplace:'aa'.replaceAll('a',()=>'$&')};
console.log(JSON.stringify(out,null,2));
