# E05 X-R04-2 hedge measurement record

Task E05, 2026-09-26. Every row below is an executed observation
against the pinned runtime unless marked reasoning. No credentials
appear here; env names only.

Environment: Bun 1.4.2 (`bun --version`, `packageManager:
bun@1.4.2`), macOS darwin-arm64, PostgreSQL 17.11 on
127.0.0.1:55433 (`CAN_TEST_POSTGRES_URL`), MySQL 8.4.11 on
127.0.0.1:3307 (`CAN_TEST_MYSQL_URL`), per
`distribution/provision-local.md`. Live legs ran in isolated
database `can_e05_run1` (`CAN_E05_DB`; minted via
`distribution/provision-local.sh db mkdb`); no other lane database
was touched. Frontend servers on loopback 18771–18782 (E05-owned,
verified free); stall-injection upstream on ephemeral loopback
ports. Committed harness: `runtime/test/http-hedge-harness.ts`
(boundary-race hedge + stall upstream),
`runtime/test/http-hedge.test.ts` (16 legs).

No production runtime file changed: the hedge is a test-harness
shape over the E04 vocabulary (no new module, no catalogue
change, no `modules.json` change). No universal SLO is claimed:
every timing assertion compares bounded against unbounded order
(sub-1500ms vs 1500ms-plus beside two-second stalls), never an
absolute latency bound.

## Measured shape: boundary-race hedge under O1

Each replica races its own `raceBoundary` against the shared
request scope (budget, disconnect/shutdown signal, escalation
sink). The primary starts at once; the secondary starts after
`hedgeDelayMs` unless the primary already won. The first settled
replica wins and the handler responds with its value; the loser is
never awaited. Terminal precedence is expired over rejected (H0d).
`abortLosers` aborts each still-pending cancelable loser at the win
through a per-replica controller shared by its boundary signal and
its native abort; SQL replicas set `cancelable: false` and run to
settlement owned. Two selected O1 sub-shapes:

- S1 let-settle (cancel-absent replicas: SQL, bare owned work):
  losers run to settlement; the response never waits because these
  replicas hold pool leases, not request-scope owner groups.
- S2 abort-loser (cancel-present replicas: fetch): losers abort
  natively at the win through the qualified E04 abort; the
  pre-response drain releases and the response stays bounded.

## Leg observations (final full run, 16/16 pass, 26s)

| Leg | What it measures | Observation |
|---|---|---|
| H0a | all-rejected contract | `failed` with both errors, no throw; 2 rejected fates |
| H0b | validation | arity/delay/bound reject with `TypeError` |
| H0c | zero-delay start | both replicas start before the first await; earliest wins |
| H0d | mixed terminal precedence | rejection lands first, report is still `unknown` (budget) |
| H1 | winner response, group-free loser | 200 `winner:1:secondary`, bounded (<1500ms) beside a 2s loser; 0 sink records; 2 fates late |
| H2 | failure absorption | primary fails fast, secondary wins; 1 rejected fate, 0 sink records |
| H3 | all-expired honesty | 200 `unknown:budget`, bounded; marker commit unknown/owned/supervisor; 2 escalations + 2 seq-linked late resolutions |
| H4 | fetch w/o abort (O2-motivating shape) | winner content, but ~2s: response waits for loser drain; upstream slow hit never aborted; 0 sink records |
| H5 | fetch with abort (O1 answer) | 200 `winner:1:fast`, bounded; upstream slow hit aborted serve-side; 1 cancel escalation + seq-linked late rejection |
| H6 | late loser failure | 200 before the 500ms loser failure; fate rejected `late-boom`, owner-observed only, 0 sink records; run clean |
| H7 | disconnect mid-hedge | client `AbortError`; handler boundary <1500ms, cause disconnect; 2 escalations + 2 owned late settlements; next request `winner:0:primary` |
| H8 | stop during hedge | live peer `unknown:shutdown`, bounded; 2 shutdown escalations; 2 late settlements; clean stop |
| H9 | SIGTERM child E2E | live peer `unknown:shutdown`; child logs `boundary:shutdown`, `wait:ok`; exit 0, stderr empty |
| H10 | in-process lease + cleanup | winner fast; leases 1 past response; close waits ~800ms for the lease, then 0/closed; post-close use rejects; 0 escalations |
| H11 | live PG lease under hedge | 200 `winner:1:fast-probe`, bounded; leases 1→0; `pg_sleep` backends 1→0; overlap query ok; 0 escalations |
| H12 | live MySQL thread under hedge | same shape: bounded, leases 1→0, `SLEEP` threads 1→0, overlap ok |

Effects accounting: H1/H3/H4/H5 start both replicas whenever the
primary misses the delay (hit counts 2/2); H5 proves the loser
wire request aborts upstream. All measured replicas are reads.

## X-R04-2 comparison verdict per dimension

| Dimension | O1 result | O2 needed? |
|---|---|---|
| Response/latency | bounded in S1 (H1/H11/H12) and S2 (H5); only the unselected no-abort fetch shape blocks (H4) | no — S2 covers it with qualified abort |
| Leases | never revoked (H10/H11/H12: 1 past response, 0 after settlement); close drains | no |
| Late-failure diagnostics | expired-then-settled via seq-linked late records (H3/H5/H7/H8); post-win rejection owner-observed (H2/H6, Jev 3/3 owner_observed) | no |
| Disconnect | both boundaries expire cause disconnect; owned settlement; server healthy (H7) | no |
| Cleanup/effects | both replicas accounted; abort observed upstream; close drains; no unhandled rejections; servers healthy after every leg | no |
| Shutdown | bounded `unknown:shutdown` to live peers; clean stop + SIGTERM exit 0 (H8/H9) | no |
| Unknown honesty | frozen markers, commit unknown, owned, supervisor escalation; expiry dominates rejection (H0d/H3) | no |

## O2 gate resolution: INACTIVE

No measured inability to satisfy W5 exists: every dimension is
green under the two selected O1 sub-shapes, so the conditioned O2
preparation return does not trigger. No supervised-loser lifetime
contract is published and no supervisor-policy code is written.

O2 trip conditions (any one reopens the gate with fresh
measurement): (1) a hedged replica path that is BOTH
drain-blocking (owner-grouped) AND natively unabortable with no
let-settle alternative — e.g. a qualified future S3 hedge need;
(2) a W5 leg requiring a bounded hedge response over such a path;
(3) a late-failure loss that drops or misreports an unknown
outcome. Absent these, O1 stands.

Boundary conditions (not claimed): S3/stream hedge is unmeasured
(S3 deadline stays W5-blocked per E07); zero hedged-write shapes
are qualified (Jev-advised `idempotent_writes` requires per-shape
idempotency proof plus H4/H5-style effects accounting — see
`e05-jev/summary.md`); header stalls still never dispatch (E04
pin, unchanged).

## Handoff to E09

O1-sufficient verdict with the two sub-shapes above. Reuse
`runtime/test/http-hedge-harness.ts` (`hedge`, `startStallUpstream`)
for any W5 hedge legs: S1 for SQL/bare replicas with
`abortLosers: false`, S2 for fetch replicas with `abortLosers:
true` and `cancelable: true`. Contract points: expired-over-rejected
precedence (H0d), owner-observed late failures (H2/H6), cancel-cause
escalation plus late rejection for aborted losers (H5), reads or
per-shape-proven idempotent writes only. No checker/emitter need:
the hedge is a transport-internal shape, not authored syntax.

## Checks

- `bun test runtime/test/http-hedge.test.ts` (live DBs,
  `CAN_E05_DB=can_e05_run1`): 16/16 pass, 140 expect calls.
  Without DB env: 14 pass + H11/H12 visibly skipped.
- Full `bun test runtime/test/` (raw exports): 974 pass / 34
  skip / 18 fail, all 18 pre-existing and E05-unrelated (17
  `mysql.*` legs needing the literal `/can_b1_03` URL shape the
  exports placeholder does not supply; 1 `owner-explicit-browser`
  leg needing the missing Playwright module — the same env
  failure E04 recorded). No other file imports the E05 harness.
- `bun run check:runtime`: green (oxlint 0/0, oxfmt clean, tsc
  clean).
- `go test ./compiler/internal/browser/ -run
  TestRuntimeInventoryMatchesBodies`: green (E05 adds test-harness
  files only, outside the inventory).
- Jev 3/3: `e05-jev/` (requests, responses, metadata, summary);
  unanimous, weakest leg 0.68 investigated in the summary.
