# E09 W5 qualification record: lifetime, reporting and storage

Task E09, 2026-09-26. Every live row below is an executed observation
against the final adapters unless marked reasoning. No credentials
appear here; env names only.

Environment: Bun 1.4.2, PostgreSQL 17.11 on 127.0.0.1:55433
(`CAN_TEST_POSTGRES_URL`), MySQL 8.4.11 on 127.0.0.1:3307
(`CAN_TEST_MYSQL_URL`), MinIO `RELEASE.2025-09-07T16-13-09Z` on
127.0.0.1:9000 (`CAN_TEST_S3_*`, bucket `can-b1-10`), per
`distribution/provision-local.md`. Live legs ran in isolated database
`can_e09_run1` (`CAN_E09_DB`; minted via
`distribution/provision-local.sh db mkdb`), under S3 prefix
`s3e09/e09harness/<run>/`, on E09-owned loopback ports 18790–18799
(all verified free before the run); no other lane database, prefix or
port was touched. Committed harnesses: `runtime/test/w5-lifetime.test.ts`
(10 legs), `runtime/test/w5-s3.test.ts` (6 legs). Test-only change: no
production runtime file, no catalogue change, no `modules.json` change.

No universal SLO is claimed: every timing assertion compares bounded
against unbounded order (sub-1500ms beside two-second stalls), never an
absolute latency bound.

## Inherited verdicts honored (not relitigated)

- X-R04-1 NEGATIVE all dialects / X-R04-3 POSITIVE (E02): every SQL
  leg runs the cancel-absent branch — boundary-returns-at-budget,
  owned-until-settlement, unknown-write, supervisor escalation. No leg
  passes a signal to a SQL boundary or calls `Query.cancel()`.
- X-R04-2 O1-sufficient / O2 inactive (E05): no supervised-loser
  lifetime used or needed by any W5-L leg.
- X-R15-1 NEGATIVE → O2 destructive (E07/E08): W5-S legs assert name
  removal, explicit discard sites and honest outcomes — never
  preservation (X-R15-2 negative row).
- X-R15-3 NEGATIVE (E07): W5-S5 pins the hung-await negative with the
  exact evidence and the verdict stays BLOCKED — it never becomes an
  accepted pass.

## W5-L verdicts (lifetime + reporting, final server/router/SQL dispatch)

| Leg | Verdict | Observation |
|---|---|---|
| W5-L1 stalled PG read over HTTP | PASS | `200 unknown:budget` bounded; `sql::query_failed {query_rows, budget}`; 1 escalation cause budget; leases 1→0; `pg_sleep` backends 1→0; late resolved; hook silent (0 reports); follow-up `/fast` ok |
| W5-L2 stalled MySQL read over HTTP | PASS | Same shape: bounded `unknown:budget`; 1 budget escalation; leases 1→0; `SLEEP` threads 1→0; late resolved; hook silent |
| W5-L3 stalled mid-flight tx write over HTTP | BLOCKED (at-budget peer response) | Boundary honest (see below); peer gated on settlement, not the bound |
| W5-L4 header stall | PASS | Partial headers 2s: dispatch fires 0 (Bun-owned pin, no Can hook) |
| W5-L5 body stalls | PASS | Over-limit body → 413 over the wire; full-headers/zero-body + shutdown → 408 bytes over the wire |
| W5-L6 disconnect into live MySQL query | PASS | Client `AbortError`; handler boundary <1500ms cause disconnect; 1 escalation; late settled; sleepers 0; leases 0; next request `settled`; hook silent |
| W5-L7 overlap on one pool | PASS | Slow + fast concurrent, both bounded <1500ms: `unknown:budget` + `fast-ok`, uncrossed; escalations 1/0 per scope; leases 0; sleepers 0 |
| W5-L8 SIGTERM child E2E | PASS | Live peer `unknown:shutdown`; child logs `boundary:shutdown`, `wait:ok`; exit 0, stderr empty |
| W5-L9 unexpected failure beside a stall | PASS | Exactly 1 redacted correlated report (`route:POST /boom`, uuid correlation) + fixed 500; stalled peer still `unknown:budget`; no secret bytes in the report |
| W5-L10 double-throwing wrapper | PASS | Exactly 1 redacted report + fixed 500; neither inner nor wrapper secret leaks |

## W5-L3 BLOCKED analysis (new E09 finding, exact evidence)

The handler boundary behaves exactly per contract: `withTransaction`
with `boundMs: 150` returns `sql::commit_unknown {transaction_id:
"1-1"}` at handler-side 155ms; 1 escalation cause budget with the
`unknown-write / owned / supervisor` marker; the tx settles owned
(late resolved at ~2067ms); the reconciliation reread finds row id 8
exactly once; the hook stays silent. The peer, however, receives the
`unknown:commit` body at 2009ms — at tx callback settlement, not at
the 150ms bound.

Mechanism (read, then measured): dispatch runs each request inside
`withScope`, and owner-core `drain` waits for every callback task
`inside` the request scope. The tx callback is a guarded task in the
tx's inner scope, which nests inside the request scope — so the
request drain (hence the peer response) waits for the tx callback
even though the boundary already returned. Pool-lease work (L1/L2/L6/L7
query legs) is unaffected because leases hang off the pool resource,
not the request scope. The measured split (handler 155ms, peer
2009ms, late record 2067ms) shows the peer releases at callback
settlement, before the late record lands.

Consequence: the overrun-tx shape is drain-blocking (owner-grouped)
plus natively unabortable — the exact shape class the X-R04-2 O2 trip
condition names (there scoped to hedged replicas; this is the
unhedged tx instance). A hung tx callback would hold the peer past
any bound; `stop` itself stays bounded via `shutdownMs` (the
in-flight peer is cut with the session, not answered — reasoning from
`server.stop`/`wait`, unmeasured for tx). Detaching tx ownership from
the request scope is an E04 semantic change with disposal/commit
implications, not a W5 leg fix — so W5-L3-at-budget is BLOCKED and
handed to IC2/preparation. The leg pins the split (handler <1500ms,
peer ≥1800ms and <8000ms) and fails loudly if the shape ever changes.

## W5-S verdicts (storage, final S3 adapters; S3-protocol scope)

MinIO caveats from E07/E08 carry over unchanged (prefix-ignoring
list, vacuous abort-204, one unreproduced H3 listing). The bucket was
left clean: 0 objects under `s3e09/`, 0 pending uploads (verified
with `aws s3api` after the final run).

| Leg | Verdict | Observation |
|---|---|---|
| W5-S1 name removal + destructive contract | PASS | `s3::cancel_upload` lookup throws; name/identity absent from catalogue; `s3::discard_upload` → void with emits `[access_denied, service_error, upload_closed]` and the destructive wording |
| W5-S2 discard-while-replacing destroys seed | PASS | Seeded 14 B + etag; wire `[PUT put-object, DELETE delete-object]`; key missing after; read → `missing_key`. Preservation explicitly disproven |
| W5-S3 concurrent observer | PASS | 2 runs: `{original, replacement, missing}` then `{original, missing}` — the transient replacement window reproduced live; never partial/mixed bytes |
| W5-S4 failed completion + orphan recipe | PASS | `discard_upload` → `service_error {InternalError, discard_upload}` after 1+3 complete attempts; orphan 1 strands; list-unfiltered + abort + re-list verifies clean; key absent |
| W5-S5a between-awaits wording | PASS | `write_stream` documents "only between awaits / never a hung await" + destructive timeout scrub |
| W5-S5b hung source.read | BLOCKED (X-R15-3) | Watchdog at 3s; zero wire ops; the deadline never fires mid-await. Recorded blocked with this exact evidence; never an accepted pass |

X-R15-3 in full: W5-S5b pins the hung-read instance on the final
adapters; E07 H2–H5 (hung flush/end/stat/abandonment, re-run green
below) pin the remainder. The W5 S3 deadline legs stay BLOCKED
pending explicit user scope return per F-R15-03/04.

## W5 validation mapping

| Requirement | Legs |
|---|---|
| Stalled SQL, bounded + escalation + owned settlement | L1/L2 (PASS) |
| Honest unknown-write outcomes | L3 content/reconcile (PASS); at-budget peer timing (BLOCKED) |
| Header/body stalls | L4/L5 (PASS) |
| Disconnect / overlap / SIGTERM | L6/L7/L8 (PASS) |
| Once-only redacted fixed-500 hook incl. double-throw | L9/L10 (PASS); L1/L2/L3/L6 prove expected outcomes stay silent |
| S3 O2 destructive/unknown evidence | S1–S4 (PASS) |
| Hung awaits | S5b + E07 H1–H5 (BLOCKED, X-R15-3) |
| Orphan/service failures | S4 + E07 F1–F7 (PASS, recipe verified) |

## Prerequisite re-runs on the final adapters (all green)

- E04: `operation/sql/action/transport-budget` 61/61; `server-budget` +
  E06 `request-report` + `request-lifetime` 38/38.
- E05: `http-hedge` 16/16, 140 expects.
- E06 hook contract: covered in the 38 above (17/17 report legs).
- E07: `s3-e07-remedy` 21/21 (60s); E08: `s3-e08-remedy` 5/5 +
  `s3.test.ts` 19/19.
- New: `w5-lifetime` 10/10 (106 expects, ~20s) +
  `w5-s3` 6/6 (47 expects) = 16/16.
- Full `bun test runtime/test/`: 1038 pass / 17 fail, all 17
  pre-existing and environmental: 16 `mysql.*` legs needing the
  literal `/can_b1_03` URL shape (verified 17/17 pass with that shape
  substituted) and 1 `owner-explicit-browser` leg needing the missing
  Playwright module — the same env failures E04/E05 recorded. No W5
  or prerequisite file fails.
- `bun run check:runtime`: green (oxlint 0/0, oxfmt clean, tsc clean).
- `go test ./compiler/internal/browser/ -run
  TestRuntimeInventoryMatchesBodies`: green (test-only files, no
  inventory change).

## Handoffs (IC2 / H lifecycle story)

- Per-leg evidence: this record + `w5-lifetime.test.ts` + `w5-s3.test.ts`.
- BLOCKED W5-L3-at-budget: overrun-tx peer response gated on tx
  callback settlement via the request-scope drain (handler 155ms vs
  peer 2009ms beside a 2s stall). Needs a preparation return on tx
  ownership vs response bound; the O2-trip shape class now has a live
  unhedged instance. Shutdown interplay reasoned, not measured.
- BLOCKED X-R15-3: hung S3 awaits unbounded (W5-S5b + E07 H1–H5);
  needs explicit user scope return. Between-awaits bound holds (D4).
- Operator orphan guidance (unchanged from E08): complete-failure
  always orphans; recipe is list-unfiltered + client-side filter +
  abort + re-list; W5-S4 verifies the recipe end to end.
- No production, catalogue, or inventory change: nothing to migrate.
