# Can implementation task list, dependency ordered

24 September 2026. This is the result of the [design preparation](can-design-preparation-2026-09-24.md). It is a **plan**, not a claim that any production change below has shipped. Can is for AI coding agents; correctness, stable contracts and observable product behavior lead, while total tokens per successful agent task is a secondary measured outcome. There are no external users or compatibility requirements. The [disposition ledger](preparation/design-disposition-ledger.md) covers every DI-01–DI-23 item; only accepted mechanisms and qualification work appear as build tasks. The [integrated action contract](preparation/integrated-action-contract.md), [product acceptance matrix](preparation/product-acceptance-matrix.md), and linked topic packets define exact semantics.

## How to execute this graph

`Depends on` is a hard prerequisite for integration or qualification, not necessarily for beginning an isolated branch or test fixture. Each task lists a suitable GPT-6 model and Codex reasoning effort: Astra for difficult contract integration and Sol for implementation. These are assignments, not proof of optimal cost. A task may be split among workers while its lead owns the stated definition of done. After T01, **T02/T03/T06/T07/T18/T19** can run in parallel. T03 unlocks **T04/T05/T08**; T05 and T06 unlock T10, while T03/T05/T06/T07 unlock T21. T10 unlocks **T11/T12/T13**; T21 unlocks T22. T09 integrates the core before T16, while the invoice and browser lanes continue on their own stated prerequisites. Do not merge two workers' compiler or catalogue edits blindly; integrate against one locked revision and rerun affected checks. The [model rationale](can-preparation-model-allocation-2026-09-24.md) applies to delegated work.

For every compiler/catalogue change, add positive, negative and diagnostic-span tests plus regenerated generated TypeScript/goldens as applicable. For every task that edits authored `runtime/` or `tools/runtime/` TypeScript, run `bun run lint:fix:runtime`, `bun run format:runtime`, `bun run check:runtime` and relevant tests; use `bun run lint:runtime --format=agent` for compact diagnostics. Do not hand-edit generated `runtime/catalogue.ts` or pinned vendor files. For observable product tasks, tests must inspect native HTTP/browser/database results, not just a Can assertion or Jev advice. Record actual model/version, compiler revision, environment and failures in each qualification report.

## Wave integration status and acceptance evidence

- [x] **T01** (wave w1) — integrated as `43625ed8ac96ffc648ed94a4c87d4f49aa434004`, cherry-pick of `07c456c73237e6e2a6c326ee35dbeadfd1170073` onto `b53030b`; no conflicts. Adds `tests/baseline/{README.md,baseline.json,registry.json,run.sh,baseline_test.go}`: dated 2026-09-24 freeze (compiler revision `b53030b`, Bun 1.4.2, Go minimum 1.25, disposable-directory policy, core regression commands mirroring CI) and a 7-case registry with creation/refactor/repair prompts, held-out variants, hidden checks and frozen trial settings (primary Sol Medium, efficient Luna Medium, Astra High escalation, five attempts). All candidate slots pending; no language mechanism credited. Verified 2026-09-24 on go1.27.1 darwin/arm64 with Bun 1.4.2+744846f84 at HEAD `43625ed`: `go test ./tests/baseline/ -v -count=1` 3/3 PASS, `tests/baseline/run.sh --smoke` failures=0, `gofmt` clean, `go vet ./tests/baseline/` clean.
- [x] **T02** (wave w2) — integrated as `5aa72149f969485931ae01cab4ce0f0b1e2ba349`, cherry-pick of `7cd6d8fbdc4998d7f7a8d40a32061785e69586ac` onto `b5e14db`; no conflicts. Implements DI-01: `BindPattern` AST node, `bind` formatter/token support, checker nominal-leaf resolution with duplicate/alternative-arm binding checks, plus `decisions.md`/`technical-spec.md` updates. Verified 2026-09-24 on go1.27.1 darwin/arm64 at HEAD `25fc3ae`: syntax `TestBindPatternNodes`, `TestBindPatternNestedAlternatives`, `TestBindFallsBackToNominal` PASS; check `TestPatternBindCapturesAtEveryDepth`, `TestPatternBindDuplicatesAndAlternatives`, `TestRawFixtureBindingRejects` PASS; full `go test ./...` PASS.
- [x] **T03** (wave w2) — integrated as `2c8a39cda67822520e3e94c9d5ab355594c20fe2`, cherry-pick of `3757b6b0895812ce90e7abd5bc7d4bbc692b033a` onto `5aa7214`; auto-merge with T02 in `syntax/ast.go`/`syntax/format.go` (disjoint regions: `Import.Dependency` vs `BindPattern`, both verified present). Implements DI-05a: canonical package-instance graph, `uses [dependency::package as local]` parse/format, manifest lineage, lock interning/divergence rejection, symbol identity and diagnostics. Verified 2026-09-24 on go1.27.1 darwin/arm64 at HEAD `25fc3ae`: 8/8 focused instance/uses/lineage tests PASS across `project`/`resolve`/`syntax`/`compiler` packages; `go test ./compiler/... ./distribution/...` PASS.
- [x] **T06** (wave w2) — integrated as `29351577da1e6db71ae17bdf558617662090b15f`, cherry-pick of `540bd4000c1b9f31587e3f867efa8e1e413cc142` onto `2c8a39c`; no conflicts. Implements DI-07 extensional closed-variant leaf-set compatibility in `types/compatibility.go`, removing the path-sensitive specialization exception. Verified 2026-09-24 on go1.27.1 darwin/arm64 at HEAD `25fc3ae`: check `TestGenericVariantDirectBridgeLeafConversion`, `TestNestedGenericVariantConversion`, `TestOptionConversion`, `TestGenericVariantIncompatibleRecords`, `TestGenericVariantExhaustiveness` PASS; types `TestGenericVariantLeafSetCompatibility`, `TestGenericVariantLeavesKeepArgumentIdentity`, `TestNestedGenericVariantLeafSetCompatibility`, `TestOptionSpecializationsStayDistinct` PASS.
- [x] **T07** (wave w2) — integrated as `ebace7403a198aa446262b10060f72baaca06baa`, cherry-pick of `7f7181a472122cf8896a08fe55901cd501e84e57` onto `2935157`; no git conflicts, with semantic alignment in `25fc3ae9c16b2c11c064974ee1ec601ac8001c73` (`box_match` fixture captures via `bind held` per DI-01; dependency-edit fixture imports `vendor::helpers` per DI-05a; T07 assertions unchanged). Implements DI-04 exported generic checking under symbolic type variables with explicit named callable/dictionary inputs. Verified 2026-09-24 on go1.27.1 darwin/arm64 at HEAD `25fc3ae`: `TestExportedGenericStructuralComposites` 5/5 subtests PASS, `TestExportedGenericDependencyEdit` PASS, full `check`/`emit`/`types` ExportedGeneric/Symbolic/TypeParam selections PASS.
- [x] **T18** (wave w2) — integrated as `28b1226d2204e915a14518eb45e9e078c34c18e4`, cherry-pick of `82a46d10ecf5172ce283219313b367ef76b02d82` onto `ebace74`; no conflicts. Corrects the stream example (exactly-once close, original-versus-close failure precedence) and adds `docs/implementation/shutdown.md` bounded shutdown policy plus `runtime/test/shutdown.test.ts`. Verified 2026-09-24 on go1.27.1 darwin/arm64 with Bun 1.4.2 at HEAD `25fc3ae`: `bun test runtime/test/shutdown.test.ts` 6 pass / 0 fail; `bun test runtime/` 1590 pass / 84 skip / 0 fail; `bun run check:runtime` (lint, format, typecheck) clean; dev-distribution `canlc assert examples/stream` passed 7/7 and `canlc run examples/stream -- /tmp/lines.txt` printed `3`.
- [x] **T19** (wave w2) — integrated as `4f5ea0edefce5761b096b720f69dae7336d693fa`, cherry-pick of `534040acab1cacf438625a4f3e9e3769a26f916a` onto `28b1226`; no conflicts. Pins Bun 1.4.2 Linux x64 on Debian 13 amd64/glibc with recorded digests (`distribution/linux/provenance.json`, `target-linux-amd64.json`) and reproducible build/install/smoke scripts. Verified 2026-09-24 on go1.27.1 darwin/arm64 (Docker, network-denied smoke) at HEAD `25fc3ae`: local archive sha256 `36368fae…` matches provenance size 36646985; `BUN_ARCHIVE=/private/tmp/bun-linux-x64.zip ./distribution/linux/run.sh` full pass — runtime-check 1.4.2 linux x64, native qualification 14/14 (78 APIs, 10 probes), process/files/crypto/sqlite examples ok, deterministic build `629494b8…`, postgres 17.11 roundtrip `7:remember`; Go `TestLinuxTargetPin`, `TestLinuxProvenanceMatchesPin`, `TestHostTargetSelection` PASS (`TestLinuxInstalledArtifactSmoke`/`TestLinuxPostgresRoundtrip` skip on darwin by design). Wave-wide: `go test ./...` PASS, `gofmt`/`go vet` clean, `cataloguegen --check` clean. One environment note: a stale gitignored `examples/crypto/dist` from a Sep-23 host build was baked into the first image build and failed the crypto leg with a dist-owner mismatch; after removing the regenerable `dist/` dirs the documented command passes unmodified.
- [x] **T04** (wave w3) — integrated as `227fe3ceb17bc7a037aefd446fbf618b9ca12c54`, cherry-pick of `aa72987862c6c8b3b3195d3562f3f3d1e7e1a046` onto `b832f28`; no conflicts. Implements DI-05b: unnumbered `error` declarations (grammar, checker, catalogue boundary, emitted metadata, runtime reporting, CLI, lock retirement checks), qualified `can.error.v2` report identity with concrete generic identity, payload redaction, retired-name ledger with predecessor-chain attribution; old numbered syntax/goldens removed (zero `error <int>` fixtures remain). Verified 2026-09-24 on go1.27.1 darwin/arm64 with Bun 1.4.2 at HEAD `cd2ec97`: project `TestFormerNumericCollisionComposesWithQualifiedIdentity`, `TestRegistryResolvesSuppliedPredecessorChain`, `TestArchivedTwoBuildReportsAttributeAcrossRename` PASS; runtime domain-report identity/redaction test PASS; full `go test ./...` PASS.
- [x] **T05** (wave w3) — integrated as `2d8e2c316313e2a658c3bc4c919fd1170e06ebc9`, cherry-pick of `a20da217b2fa48851ad7ddf33e5a5e5f58cfb5c9` onto `227fe3c`; auto-merge with T04 in `syntax/ast.go`/`syntax/declarations.go`/`syntax/format.go` (disjoint regions: owner-record nodes vs unnumbered-error grammar, both verified present). Implements DI-02 `owner record`: owner-only construction/update/field-read, exported projection, public leaf match/equality, and rejection of generic codec/schema derivation reaching an owner record even in its package. Verified 2026-09-24 on go1.27.1 darwin/arm64 at HEAD `cd2ec97`: check `TestOwnerRecordBoundaryPositives`, `TestOwnerRecordForeignNegatives` (7/7 subtests), `TestOwnerRecordInPackageCodecRejection`, `TestOwnerRecordFixtureAdmission` PASS; syntax/emit/resolve/types owner-record tests PASS (`TestOwnerMarkingAndSchemaRejection`, `TestOwnerReachableThroughNestingIsRejected`, `TestOwnerRecordLowersThroughNominalHelpers`, `TestOwnerRecordConstructorConfinedToDeclaringPackage`, `TestOwnerRecordExportedProjectionStaysNametypeable`, `TestOwnerRecordDeclaration`, `TestOwnerRecordRequiresRecordKeyword`).
- [x] **T08** (wave w3) — integrated as `cd2ec974620c2d8afc7fabb54677ee0c141b91af`, cherry-pick of `2eacc4cbf319bee13077769697d9ccfbd01a9b95` onto `2d8e2c3`; auto-merge in `check/program.go`, `resolve/symbols.go`, syntax files, docs and runtime tests, with one content conflict in `syntax/token.go` resolved as the keyword union (`owner` + `scenario link`, both verified present). Implements DI-06: same-owner lexical fixture selection and bounded exported `scenario` declarations with exact static path/case rows plus `using scenario` link rows. Verified 2026-09-24 on go1.27.1 darwin/arm64 with Bun 1.4.2 at HEAD `cd2ec97`: check scenario tests 9/9 PASS (`TestScenarioLinkResolves`, `TestScenarioLinkSamePackageNeedsNoExport`, `TestScenarioLinkStale`, `TestScenarioLinkRespectsAlias`, `TestScenarioLinkAmbiguous`, `TestScenarioPlacementRejects`, `TestScenarioTwoSequentialLinks`, `TestScenarioUseRowInheritsTag`, `TestScenarioMalformedSidecar`); syntax/emit scenario tests PASS; `bun test runtime/test/scenario-links.test.ts` 9 pass / 0 fail. Wave-wide: `go test ./...` PASS, `bun test runtime/` 2133 pass / 112 skip / 0 fail, `bun run check:runtime` (lint, format, typecheck) clean, `gofmt`/`go vet` clean, `cataloguegen --check` clean.
- [x] **T09** (wave w4) — integrated as `9fcc1ae3ad8c70c68f7f0894e6919547b609acd4`, cherry-pick of `4f5ae866b19c674ab923eb67b859fab2f75d1bce` onto `0b0d2dd`; no conflicts. Adds cross-feature core-integration suites only (`check`/`emit`/`syntax` `core_integration_test.go`, 875 lines): owner record through generic/package boundary, variant leaf across imports, fixture link after alias rename (plus two-links-through-alias), error identity across twin instances and after lineage change, integrated lower/round-trip. Verified 2026-09-24 on go1.27.1 darwin/arm64 with Bun 1.4.2 at HEAD `0683c17`: all 6 `TestCore*` check tests plus `TestCoreIntegratedProgramLowers` (emit) and `TestCoreIntegratedSurfaceRoundTrip` (syntax) PASS.
- [x] **T10** (wave w4) — integrated as `2c8c1edfb7996acd664db29d2587a1708e5e2a60`, cherry-pick of `64a653ae88aafdc8d38f91838af8896fc5e73d42` onto `9fcc1ae`; no conflicts. Implements source `action` declarations and checked catalogue symbols across 17 files (checker, emitter contract table, syntax parse/format, symbol resolution, docs): POST/GET method, exact/captured paths, body modes, exhaustive leaf-to-status cases, static handler/field references, duplicate/route-shape collisions, owner/fetch/private-handler rejections. Verified 2026-09-24 on go1.27.1 darwin/arm64 at HEAD `0683c17`: all 12 `TestAction*` check tests PASS (including 35-subtest `TestActionRejects`), emit `TestActionEmissionFreezesContractTable`/`TestActionEmissionOmitsEmptyTable` PASS, all 5 syntax `TestAction*` parsing/format tests PASS.
- [x] **T21** (wave w4) — integrated as `0683c17396cd020e77fa8bf7f91b175731e83db6`, cherry-pick of `01d3ec037a43307545c25fae528aaf71aa2a3be8` onto `2c8c1ed`; auto-merge in `emit/program_state.go` (disjoint regions: T10 action constants vs T21 browser state gating, both verified present). Implements `canlc build --target browser` across 18 files: distinct main-thread root/profile, transitive browser capability closure with callable/generic/initializer path rejection, compiler-produced content-addressed same-origin asset, shared strict wire codecs, emitted import/sourcemap audit. Verified 2026-09-24 on go1.27.1 darwin/arm64 with Bun 1.4.2 at HEAD `0683c17`: `browser` package (19 tests), `driver` browser tests (2) and emit browser tests (4) PASS; `TestBrowserWireCodecParity` PASS; `bun test runtime/test/browser-wire.test.ts` 1 pass / 0 fail; `TestBrowserBuildTarget` skips by design without `CAN_BUN_ARCHIVE` (staged browser execution). Wave-wide: `go test ./...` 19 ok / 0 fail, `bun test runtime/` 2134 pass / 112 skip / 0 fail, `bun run check:runtime` (lint, format, typecheck) clean, `gofmt`/`go vet` clean, `cataloguegen --check` clean.
- [x] **T11** (wave w5) — integrated as `c35a6e7320f95a26a7a30dd6a6bab43e7cebf3f3`, cherry-pick of `43c72c953cbd03d5b4b2b9f39ff9579dc5ddd4aa` onto `31b1fc2`; no conflicts. Implements DI-09 typed `str`/int64 path captures, canonical URL builder, guarded native route table, method-first/static-within-method dispatch, collision checks and 400/404/405 classification with `Allow` header, reentering the existing asset/ingress/scope/header lifecycle. Verified 2026-09-24 on go1.27.1 darwin/arm64 with Bun 1.4.2 at HEAD `cea1500`: `bun test runtime/test/action-routes.test.ts` 13 pass / 0 fail; check `TestActionStaticCaptureOverlapAllowed`, `TestActionGetPostSharePath`, `TestActionCrossPackageHandler` PASS.
- [x] **T12** (wave w5) — integrated as `c8a27700d0c3321808f084fd2a666c94dd1b7263`, cherry-pick of `323098421f7c063b5c61e34fd6476eb606863544` onto `c35a6e7`; `actions.go` auto-merge (T11 captures + T12 form body), one content conflict in `platform-testing-spec.md` resolved as the T11 dispatch paragraph followed by the T12 keyed-row section (both verified present). Implements DI-09b bounded `form::rows<line_wire>`/`form::rejected<T>` HTML action adapter: one native FormData parse, exact `lines_order`, 64-row/2048-byte limits, duplicate/unknown/partial rejection with known-raw retention, three-callable `http::serve_form_action` binding, checked name builders, P9-escaped rendering. Verified 2026-09-24 on go1.27.1 darwin/arm64 with Bun 1.4.2 at HEAD `cea1500`: check `TestForm*` 7/7 PASS, emit `TestFormActionEmissionFreezesRowsSchema`/`TestFormServeEmissionSplicesAdapterContract` PASS, `bun test` form-action 7 pass / form-names 3 pass / form-rows 12 pass / 0 fail.
- [x] **T13** (wave w5) — integrated as `b4051c479226f58901dc12b3f9ebbe0d36863fe2`, cherry-pick of `0714ce3599dfce59bf522fe2baf2c65ff404bee4` onto `c8a2770`; `actions.go` auto-merge (captures + form + JSON modes), one content conflict in `platform-testing-spec.md` resolved keeping the T11+T12 text then the T13 JSON paragraphs (all verified present). Implements JSON POST-save and bodyless GET-load action modes with typed `action::request`/`action::response` metadata: 8192-byte POST limit, media-type gate, no GET body, finite case statuses, named transport/abort/codec/unexpected-status failures. Verified 2026-09-24 on go1.27.1 darwin/arm64 with Bun 1.4.2 at HEAD `cea1500`: check `TestActionJSON*`/`TestActionPostSaveChecks`/`TestActionGetLoadChecks` PASS, emit JSON emission tests 2/2 PASS, `bun test runtime/test/action-json.test.ts` 14 pass / 0 fail.
- [x] **T16** (wave w5) — integrated as `acc167ead7ef48e74f86093a631f7c1427b748db`, cherry-pick of `29e6e4bae874e0eb17df5b2130a814bcbe6ffca8` onto `b4051c4`; no conflicts. Builds the signed provider webhook slice (`examples/webhook/`, `tests/integration/webhook_test.go`): exact-byte signature check, bounded body, idempotent ledger and outbox transaction, `commit_unknown` lookup, durable attempts, explicit receiver duplicate policy, provider fixture with crash/replay fault injection. Verified 2026-09-24 on go1.27.1 darwin/arm64 with Bun 1.4.2 at HEAD `cea1500`: check `TestSQLTransactionAccepts`/`TestSQLTransactionOptionalPopulatesOptionIdentities`/`TestSQLTransactionRejects` PASS; `TestWebhookSliceLive` skips by design without `CAN_BUN_ARCHIVE` (staged execution); `go test ./tests/integration/` ok.
- [x] **T22** (wave w5) — integrated as `cea1500d43ebb6b9c0d9a4c9cd4572958134b5ff`, cherry-pick of `0581011bbc5e117e4c0c7d1497e1204a09eb00b6` onto `acc167e`; 7 conflicts resolved preserving both contracts: `check/program.go` union (`Forms` + `BrowserStates` fields, form-generic + browser-state/listener admission, `T22` task tag), `emit/runtime_bindings.go` union (form + browser bindings), catalogue mirrors regenerated via `cataloguegen` from the merged `catalogue.json` (new source SHA `44415d98…`), inventory counts updated to the merged totals 36 packages / 101 types / 108 errors / 279 operations / 10 natives. Implements the bounded explicit browser catalogue: opaque handles, safe DOM/text/attributes/focus, event snapshots, view-scoped disposal, versioned state replacement, static literal + runtime dynamic admission. Verified 2026-09-24 on go1.27.1 darwin/arm64 with Bun 1.4.2 at HEAD `cea1500`: check `TestBrowser*` 4/4 PASS, emit browser tests 6/6 PASS, `catalogue`/`browser`/`types` packages ok, `bun test runtime/test/browser-dom.test.ts` 13 pass / 0 fail. Wave-wide: `go test ./...` 19 ok / 0 fail, `bun test runtime/` 2196 pass / 112 skip / 0 fail, `bun run check:runtime` (lint, format, typecheck) clean, `gofmt`/`go vet` clean, `cataloguegen --check` clean.
- [x] **T14** (wave w6) — integrated as `b228dc6b80e1aa972e4356ac57a4b777b872a5d6`, cherry-pick of `e0aab95994a25db3920fb5a29de8115d75a22e45` onto `e4fc13d`; no conflicts. Builds the invoice protected read/write operations and three thin action handlers (`load_invoice`, `save_invoice`, `save_invoice_form`) in `examples/invoice/` (`model.can` 578 lines, `records.can` 176 lines, `web.can` 770 lines, `schema.sql` 37 lines): actor derived from authenticated state, membership/invoice/revision checked at the protected write, durable `invoice_replay` actor+invoice+operation-ID/digest ledger; plus `$canForm`/`$canFormActions` state values reaching authored server/browser modules, router action-identity suffix agreement (frozen qualified site vs authored spelling), and `runtime/modules.json` dependency updates. Verified 2026-09-24 on go1.27.1 darwin/arm64 with Bun 1.4.2 at HEAD `0115f36`: emit `TestFormStateValuesReachAuthoredModules` PASS, `bun test runtime/test/form-action.test.ts` 7 pass / 0 fail, `canlc inspect-project examples/invoice` exit 0 with 3 actions, `schema.sql` loads in sqlite (5 tables incl. `invoice_replay`).
- [x] **T23** (wave w6) — integrated as `0115f36496fae2ede08b452838cde6a4a811295f`, cherry-pick of `844bd0c3076852c80ac86c5db59fb513e875b385` onto `b228dc6`; auto-merge in `emit/browser.go`/`emit/program_state.go` (disjoint regions: T14 form state-value names + T23 fetch state declaration/initialization/imports, both verified present). Finishes browser-side checked JSON POST-save and GET-load lowering with same-origin/CSP asset integration across 24 files: `check/action_fetch.go` client resolution with cross-target contract agreement (unknown/renamed action, method mismatch, result/wire agreement, capture arity/type, GET bodyless, POST body required), `emit/fetch_action.go` + `runtime_fetch.go` + `ir/fetch_action.go` frozen fetch-contract splicing, catalogue `fetch` entries with regenerated `generated.go`/`runtime/catalogue.ts`, `platform/action-json.ts` outcome lowering distinguishing transport/abort/codec/unknown-status failures from finite domain cases. Verified 2026-09-24 on go1.27.1 darwin/arm64 with Bun 1.4.2 at HEAD `0115f36`: check `TestFetchJSONClientsResolve`/`TestFetchJSONFailureBound`/`TestFetchJSONCrossPackageName`/`TestFetchJSONPostRejectsFormAction`/`TestActionFetchHandlerRejected` and 13-subtest `TestFetchJSONRejects` PASS, emit `TestFetchEmissionSplicesClientContracts`/`TestFetchEmissionCrossTargetContract`/`TestFetchEmissionTagsProducingOperation` PASS, catalogue `TestFetchJSONCatalogueContract` PASS, `bun test runtime/test/action-fetch.test.ts` 6 pass / 0 fail. Wave-wide: `go test ./...` all ok, `bun test runtime/` 2202 pass / 112 skip / 0 fail, `bun run check:runtime` (lint, format, typecheck) clean, `gofmt`/`go vet` clean, `cataloguegen --check` clean.
- [x] **T15** (wave w7) — integrated as `03073854bc6f0d57a8a0a2f7c58597d11b6cca7f`, cherry-pick of `d4891b08f7e9d65415fedf1e2730be19f9dfb64b` onto `696ae92`; no conflicts. Delivers the server-rendered invoice edit form: new `render` package (`examples/invoice/src/render/render.can`, 173 lines) with safe HTML builders, HTMX wiring and values escaped at serialization, `web.can` notice/edit-page routes answering truthful statuses with a content-free 500 fallback, key-shape validation in `model.can` rejecting unmintable row keys (`bad line key`) so every stored row stays form-nameable, plus live HTTP (`TestInvoiceFormLive`, actual 200/422/409/403/503 fragments with row evidence) and real-browser (`TestInvoiceBrowser`, playwright `tests/integration/browser/invoice.mjs`, 234 lines: 200/422 swap with focus/announcement, 409/403/503 never swap, target-absence leg) companion scenarios. Verified 2026-09-24 on go1.27.1 darwin/arm64 with Bun 1.4.2 at HEAD `ad1660d`: `canlc inspect-project examples/invoice` exit 0 with 4 packages incl. `render`, repo `runtime/test/html.test.ts` 9 pass / 0 fail incl. the new swap-policy test (`the emitted HTMX config swaps exactly 200-399 and 422`); `TestInvoiceFormLive`/`TestInvoiceBrowser` skip by design without `CAN_BUN_ARCHIVE` (staged execution).
- [x] **T24** (wave w7) — integrated as `ad1660d6a03d442ea7c16220fe716905c4bcc737`, cherry-pick of `952ac8cf04140ee5c8c278f5a12435007f71a5e8` onto `0307385`; auto-merge in `runtime/modules.json` (T15/T24 added the identical `platform/action-json.ts` block; git deduplicated it to a single entry and kept T24's `../assert/context.ts` dependency — verified one entry, valid union). Authors the editable invoice grid in Can (`examples/invoice-grid/`: `model.can` 1086 lines, `records.can` 150 lines, `web.can` 1034 lines): browser assertion elision admitting the four opaque handles (`app`/`view`/`node`/`state`) in `check/http.go` + `emit/expressions.go`, scope-brand harness token in `assert/context.ts` with browser ops failing elided handles closed as disposed, same-origin fetch credentials for protected actions. Verified 2026-09-24 on go1.27.1 darwin/arm64 with Bun 1.4.2 at HEAD `ad1660d`: check `TestBrowserElision*` 3/3 PASS, emit `TestBrowserElisionLowersScopeRequest` PASS, driver `TestInvoiceGrid*` 4/4 PASS (checks, browser closure with server-project rejection, cross-target contract, browser emission), `bun test` credentials 1 pass + browser-assert 3 pass / 0 fail, `canlc inspect-project examples/invoice-grid` exit 0 with 3 packages; `TestInvoiceGridStagedBrowserBuild` skips by design without `CAN_BUN_ARCHIVE`. Wave-wide: `go test ./...` 19 ok / 0 fail, `bun test runtime/` 2207 pass / 112 skip / 0 fail, `bun run check:runtime` (lint, format, typecheck) clean, `gofmt`/`go vet` clean, `cataloguegen --check` clean.

| ID · owner | Depends on | Work, outputs and definition of done |
| --- | --- | --- |
| **T01 · Sol High** | none | Freeze a dated implementation baseline and repeatable test harness: supported Bun/compiler versions, disposable build directories, core regression commands, exact current-vs-new diagnostic fixtures and case registration for agent comparisons. Produce the fixture registry with creation/refactor/repair prompts, held-out variants, hidden checks and model/effort settings before trials. The [evaluation protocol](preparation/evaluation-protocol.md) governs success and token accounting. No language mechanism is credited by a pilot. |
| **T02 · Sol High** | T01 | Implement DI-01 parser/checker semantics: bare ordinary-data identifiers must resolve to checked nominal leaves; `bind name` explicitly captures at every depth; `_`, `...rest` and error `as` retain their scoped roles. Update AST, coverage/exhaustiveness, duplicate/alternative-arm binding checks, formatter and diagnostics. Pass final-arm `decliend`, nested `paidd`, type-refactor and positive `bind` fixtures, including hidden-field cases. Regenerate parser/goldens and document the syntax. |
| **T03 · Astra High** | T01 | Implement DI-05a canonical package-instance graph and `uses [dependency::package as local]`. Manifest declares project/instance lineage IDs; lock pins content/direct edges, interns identical paths and rejects divergent same-lineage nodes. Update loader, symbol identity, visibility, source maps and diagnostics. Two unchanged libraries exposing `model` and overlapping errors compose; undeclared transitive/private imports fail; alias edit, unrelated graph growth and checkout relocation preserve identity. See [package/error contract](preparation/package-error-contract.md). |
| **T04 · Sol High** | T03 | Implement DI-05b unnumbered application `error` declarations and qualified `can.error.v2` report identity. Update parser, checker, catalogue boundary, emitted metadata, runtime reporting, CLI and lock retirement checks. Preserve payload redaction and concrete generic identity. Test two libraries with former numeric collision, active/retired disjointness, supplied predecessor chain, rename identity and archived two-build reports; remove old numbered syntax/goldens rather than preserving it. |
| **T05 · Astra High** | T03 | Implement DI-02 `owner record` from [owner contract](preparation/owner-record-design.md): owner-only construction/update/representation, exported projection, public leaf match/equality, and rejection of generic codec/schema derivation reaching an owner record even in its package. Update checker, emit, package visibility, codec and fixture admission. Pass the constructor/`with`/field-read/JSON-decode/fixture negative matrix and real wire-decode-to-owner-factory positives; a typed ID never grants authorization. |
| **T06 · Sol Medium** | T01 | Implement DI-07 extensional closed-variant leaf-set compatibility, removing the path-sensitive specialization exception. Preserve distinct generic record and leaf identities, exhaustive matching and codec behavior. Run the [16-case matrix](preparation/variant-matrix-probe.md), nested variants, `option<T>`, direct/bridge/leaf assignments, and negative incompatible records. Regenerate diagnostics and goldens. |
| **T07 · Sol High** | T01 | Implement DI-04 exported generic declaration checking under symbolic type variables with explicit named callable/dictionary operation inputs; preserve local specialization-checked templates. Update checker/interface metadata, diagnostics and generated calls. A dependency body edit adding unsupported `+` fails at the exported declaration; adding a written callable diagnoses callers and restores exact behavior. No implicit trait search or new finite error-set kind. |
| **T08 · Sol High** | T03 | Implement DI-06 same-owner lexical fixture selection and bounded exported `scenario` link syntax from [fixture contract](preparation/fixture-scenario-design.md). Preserve real helper body, exact callee/path checks, participant FIFO, root/table/invocation isolation and truthful evidence. Test caller-label rename, stale/ambiguous/unused links, concurrent roots, two sequential links and malformed sidecars. Keep whole-helper stubs as caller-unit tests. |
| **T09 · Astra High** | T02–T08 | Integrate core semantics across compiler, runtime metadata, package graph, formatter, CLI diagnostics, public docs and examples. Run full compiler/runtime regression and cross-feature negatives: owner record through generic/package boundary, variant leaf across imports, fixture link after alias rename, error report after lineage change. Regenerate outputs once from the integrated source; record any intended source break. **Gate 1/2 core contract integration** is done only when these results are reproducible. |
| **T10 · Astra High** | T03, T05, T06 | Add source `action` declarations and checked catalogue symbols from the [integrated specification](preparation/integrated-action-contract.md): POST/GET method, path, captures, input/body/result/cases metadata, exhaustive finite mapping, static field/action references, duplicate declarations, body-mode and callback diagnostics. Begin with the exact-path parser/type contract but make the selected captured paths the public invoice declarations; no compatibility route. Emit metadata consumed by server and browser adapters. Changing path, method, wire field or result leaf must diagnose or rebuild every statically linked use. |
| **T11 · Sol High** | T10 | Implement DI-09 typed `str`/int64 path captures, canonical URL builder, guarded native Bun route table, method-first/static-within-method dispatch, collision checks and 400/404/405 classification. Native callbacks must reenter existing security/body/scope/asset/header lifecycle. Run the [82-assertion trial cases](preparation/route-capture-design.md) as production tests on pinned Bun and later Linux; malformed escapes/dot/encoded separators never reach the protected handler. |
| **T12 · Sol High** | T10 | Implement DI-09b bounded `form::rows<line_wire>` and `form::rejected<T>` in the HTML action adapter. Use one native FormData parse, checked row names, exact `lines_order`, 64-row/2048-byte limits, duplicate/unknown/partial rejection and known-raw retention. Bind three server callables: valid-wire handler, outcome renderer and structural-422 renderer. Add checked field/row-name builders and safe HTML rendering. Pass the [12 native cases](preparation/keyed-row-probe.md), form-refactor diagnostics and real browser 422 text/order tests. |
| **T13 · Sol High** | T10 | Implement separate JSON POST-save and bodyless GET-load action modes and typed `action::request`/`action::response` metadata. Use existing exact JSON codec contract, native Bun Request/Response and browser Fetch; reject form-only rows/owner records as JSON wire types. Enforce POST 8192-byte limit, media type, no GET body, finite actual statuses and named transport/abort/codec/unexpected-status failures. Test route/body/leaf changes, malformed and duplicate JSON, int64/finite-number parity and no HTML-as-result decoding. Browser Fetch wiring finishes with T23. |
| **T14 · Astra High** | T05, T11, T12, T13 | Build the invoice protected read/write operations and their three thin action handlers against a real disposable database. Server actor is derived from authenticated state; membership, invoice/tenant and revision are checked at the protected write. Foreign and nonexistent resources share nondisclosing 403 behavior. Add durable actor+invoice+operation-ID/digest replay ledger for grid writes in the same transaction; same payload replays once, changed payload conflicts. Map validation to the five HTML and five JSON save cases, plus the three typed GET load cases, including raw rejected form text and typed JSON wire results. Test revocation/concurrency, cross-tenant denial and lost acknowledgement after commit. |
| **T15 · Sol High** | T11, T12, T14 | Deliver server-rendered invoice form with safe HTML builders, escaped values, checked field names/URL and actual 200/422/409/403/503 fragments. Qualify HTMX swap policy against a real browser, including stale-422-to-503 transition, target absence, focus and error announcement. Ensure no success-code rewrite, data leakage or draft loss; add live HTTP/DOM/DB companion scenarios. This is the invoice half of **Gate 3**. |
| **T16 · Sol High** | T09 | Build the signed provider webhook slice using current native HTTP/crypto/SQL primitives: exact-byte signature check, bounded body, idempotent ledger and outbox transaction, `commit_unknown` lookup, durable attempts and explicit receiver duplicate policy. Add a provider fixture, crash and replay fault injection, and inspect rows and outbound attempts. Do not add mutation `RETURNING`, queue or billing syntax absent a separately registered failing case. This is the webhook half of **Gate 3**. |
| **T17 · Astra High** | T15, T16 | Run the complete server product matrix on real HTTP, browser and database surfaces. Include route/field/case/error/reorder edits, foreign/nonexistent and stale writes, malformed keyed rows, unswapped-status regression, duplicate webhook, crash and uncertain commits. Report what passed and failed; qualify **Gate 3** only when external effects match the [acceptance matrix](preparation/product-acceptance-matrix.md). |
| **T18 · Sol High** | T01 | Correct the stream cleanup example/claim and test original-versus-close failure precedence, exactly-once close, scoped-result rejection, native race winner/empty behavior, pending losers and operation-specific abort support. Add a bounded shutdown policy and tests for deadline, disconnect and owned work. Keep DI-12/15/18 semantics as selected; no general `finally`, cancellation or consumed-fault feature is implied. |
| **T19 · Sol High** | T01 | Pin and package official Bun 1.4.2 Linux x64 on Debian 13 amd64/glibc with recorded image/archive digests. Build, install and smoke the generated Can distribution; verify process, file, crypto, SQL and native API behavior on the installed artifact. Produce reproducible build/install commands and provenance report. This can proceed beside language work but final qualification waits for T17/T18. |
| **T20 · Astra High** | T17, T18, T19 | Execute invoice, webhook and shutdown fault suites on the installed Linux artifact. Test client disconnect, slow dependency, pending loser, termination deadline and read-plus-close failure. Record host bound, abortable operations and owned settling work. **Gate 4 server-driven SaaS** passes only with actual Linux results and explicit limitations. |
| **T21 · Astra High** | T03, T05, T06, T07 | Implement `canlc build --target browser` as a distinct main-thread root/profile, with transitive browser capability closure and a compiler-produced content-addressed same-origin asset. Check callable/generic closure paths, exclude SQL/process/files/secrets/server crypto and audit emitted imports/sourcemaps. Share locked pure packages and strict wire codecs; test Bun/browser int64, finite floats, variants, option, duplicate/unknown fields and owner wire boundaries in named browsers. No arbitrary authored JS/TS or worker profile. |
| **T22 · Sol High** | T21 | Implement the bounded explicit browser catalogue in [browser design](preparation/browser-target-design.md): opaque app/view/node/state handles, safe native DOM/text/attributes/focus, event snapshots, view-scoped listener/timer disposal and versioned immutable state replacement. Native DOM/Event/AbortController operations do the work; no virtual DOM/reducer/reactive runtime. Add static literal and runtime dynamic admission checks, missing-root/disposed failures and leak tests. |
| **T23 · Sol High** | T13, T21 | Finish browser-side checked JSON POST-save and GET-load URL/request/Fetch/response lowering and same-origin/CSP asset integration. Validate method, codec, size, actual status and operation ID; distinguish transport/abort/codec/unknown-status failures from finite domain cases. Cross-target contract edits must diagnose or regenerate both sides; GET must not carry a request body. Browser cancellation must not be interpreted as server rollback. |
| **T24 · Astra High** | T14, T22, T23 | Author the actual editable invoice grid **in Can**: authorized typed initial GET/reread, immutable draft transitions, keyed row reorder, exact totals, keyboard/focus/announcement behavior, optimistic display, app-owned identified save, version-checked late outcomes, and view-owned disposal. Keep an in-view draft while offline, show unsaved/failed/uncertain state, and permit explicit identical-ID replay or authorized reconciliation; no reload durability or automatic queue promise. Test pending edit, 409/422/403/503, lost response, reordered response, remount and changed rows without clobbering newer edits. |
| **T25 · Astra High** | T20, T24 | Qualify the broad Can-authored frontend gate in a named browser/version matrix against the installed supported server. Inspect DOM, keyboard focus, live announcements, network bytes, database rows, listener/timer leaks, capability rejection and wire-codec parity. Inject offline/slow/reconnect, failed save after commit and navigation/disposal. A generated foreign-client bridge or HTMX-only page does not satisfy this task. |
| **T26 · Sol Medium** | T09, T17, T20, T25 | Update authoritative syntax/catalogue/runtime documentation, examples, generated reference/goldens, supported-platform statement and recipe for both action modes. Correct the stream example; state precise SQL guarantees, race/shutdown semantics, form/JSON/owner boundaries and the initial offline limit. Register and run held-out AI-agent creation/refactor/repair comparisons for new language mechanisms against best current idioms using the T01 protocol; report success and whole-task tokens without claiming significance from small samples. Defer candidates that fail the mechanism acceptance rule. |
| **T27 · Astra High** | T26 | Final release audit: each DI inventory row has its accepted/retained/deferred disposition; all accepted items map to tests/docs, no deferred mechanism slipped in, no generated artifact is stale, runtime lint/format/check and relevant tests pass, and Gate 1–5 evidence is linked. Publish the exact narrower server-driven and broader Can-authored recommendation only for the platforms and browser versions that passed. |

## Deliberately outside this implementation graph

DI-03 finite error-set parameters, DI-08 capture-binding syntax, DI-14a mutation `RETURNING`, DI-17 new native AI wrapper/batch/callable forms, DI-18 static escape/consumed-fault additions, DI-19 unrestricted SDK imports, DI-20 bulk immutable collections, DI-22 named-field constructors and DI-23 multiline grammar remain deferred pending the [ledger's specific reopening evidence](preparation/design-disposition-ledger.md). DI-21 billing/calendar is a later policy-explicit library/example, not a blocker for the demonstrated invoice/webhook/grid gates. DI-10 declarative markup and typed DOM-target scopes remain unselected; existing safe builders and concrete accessibility repairs are in T15/T22/T24. Current SQL statement checks and runtime row validation are described honestly; versioned schema proof is not silently added. Agent comparisons may validate the selected design or trigger a revised design decision, but implementation workers do not invent these mechanisms to make a test pass.
