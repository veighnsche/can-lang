# Final coordinator check before dispatch

The independent final audit cleared the complete nine-question packets. The coordinator then made one further equivalent change to all three: `aggregates.exact_patterns` explicitly covers every dispatch of an explicit declared error bound, including applicable coordination handlers, while preserving plain race's single implicit aggregate arm. The motivating fixture uses those coordination consumers, so ordinary-call-only wording would understate the proposal.

Manually compared all three replacement descriptions before sending. Each contains exactly the same alternatives and constraints: concrete nominal specialization patterns; ordinary and applicable coordination error dispatch; no change to plain race's implicit aggregate arm; invariant arrays; explicit element mapping only when a common aggregate is required. Every sentence was rewritten. No other field changed after the independent final audit.

The complete packets therefore retain the same nine choices and 37 options; all 56 corresponding explanatory fields are different. The preceding independent audit records full-packet semantic checks. This supplement records the scope correction and final bytes; earlier audit hashes refer to earlier unsent drafts.

Final request SHA-256:

- 1: `6421856e941a5accf102dbad6da58dcc49c7cb367e508c4a68ce472f60a7eb0b`
- 2: `9a595b89946c1786d36547b18408882f495eb7f0f78e5149710451ba70cc5eff`
- 3: `611f9a617175be4b41ced68aa3fcb0539ce005b85992bb570a0dd68ef7615d63`

The normalization question contains two viable structured single-error designs: reuse the seven original typed payloads, or define seven separate public detail-record leaves retaining the same fields. Other options intentionally include proposals from the earlier reviews that do not meet all supplied requirements. Agreement can reject those proposals but cannot prove one structured representation is uniquely correct. Option order is not randomized and wording variation does not establish bias elimination.
