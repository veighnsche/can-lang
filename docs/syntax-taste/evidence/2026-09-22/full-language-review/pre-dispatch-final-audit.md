# Final pre-dispatch packet audit — 2026-09-22

**Verdict: dispatch-ready for the required three fresh Jev consultations.** I found no remaining material mismatch among the three full requests. This is a read-only packet audit; no consultation was sent and no request file was edited.

## Exact files audited

| Packet | SHA-256 |
|---|---|
| `docs/syntax-taste/evidence/2026-09-22/full-language-review/request-1.json` | `7f02199ba9b4f97d4e9fc4cefff59ef442c7f970c95d7ddcaf45db9d063c8596` |
| `docs/syntax-taste/evidence/2026-09-22/full-language-review/request-2.json` | `11372ca07b644e106a8710fc7453eeaaadaa94c4cc30986dfb1ade6467cef90c` |
| `docs/syntax-taste/evidence/2026-09-22/full-language-review/request-3.json` | `64cc74b2a7196e5c9a875ceb5e5c5fc0290c38d0bb11404a223c6a71d8ff9684` |

All three parse as JSON, specify `jev-latest`, and have nine `choice` questions in identical order with identical option keys (37 options total, including five for `normalization`). Each has 56 nonempty explanatory prose fields: one context/constraints field, nine evidence fields, nine question instructions and 37 option descriptions. No corresponding prose field is verbatim identical between any pair of packets. Exact technical identifiers and schema keys recur appropriately. This machine check confirms structural and field-level rewriting; the semantic comparison below is manual.

## Material equivalence

- **Shared facts/constraints:** Each request says Can is implemented, emits TypeScript for bundled Bun, has zero external users and no compatibility duty, and should reuse native JS/Bun with contract/immutability adapters. Each frames the output as advice, not implementation authorization. The fixed constraints are language-owned normalization of the seven HTTP/codec errors at fetch/judge wrapper boundaries, no individual `noul`/`choice`/`score` wrappers, inherited omissions with latest override priority, error arms before final `ok`, and no nesting-reduction acceptance test. All three now specify a single judge POST, whole-answer validation and source-ordered handlers. No packet imports external research or claims Jev can prove correctness.
- **Normalization:** The same eight-fetch counts (63 repeated infrastructure bound entries, 56 forwarding arms), seven exact raw payload signatures, mode-specific reachability including no intrinsic status error for envelope fetch, private diagnostic cause, native-origin phase restriction, and passthrough of AI/handler/standard outcomes appear throughout. The new `dedicated_details` option is equivalent across packets: one default `http::request_failed`; seven **new** nominal detail-record leaves in a closed variant, retaining every original payload field; original occurrence kept privately; native preparation/transport/codec mapping only; other outcomes unchanged. It is distinct from `structured_single`, which reuses the seven original nominal payload types in a closed detail variant. Both are credible typed single-error alternatives. `alias_only`, `opaque_single` and `defer` retain their respective meanings.
- **Inheritance:** Each presents one-parent flattening with exact typed error keys, inherited omissions, most-derived/last override, explicit delegation to the inherited handler, one dispatch without recursive recatching, cycle/local-duplicate rejection, and a public effect bound recomputed from effective handlers and passthrough outcomes. Fixed input/result signatures, recovery to that result or declared domain error, no implicit retry, and escape of a chosen-handler failure are in all three contexts. The nested-catch, multiple-base and defer alternatives remain equivalent.
- **Other seven choices:** `state_calling` retains explicit marked inputs vs grouped callable types vs implicit final record; prior grouped syntax, empty-state call and field-name/serialization constraints match. `error_polymorphism` retains finite effect-set parameters with callback/expected-contract constraints, union/subset/complete-handler subtraction, handler effects and unresolved-set rejection, versus fixed aliases/body inference/defer. `aggregates` now consistently means exact generic **completion-error arm** patterns while arrays stay invariant; global closed failure set, covariance and defer are unchanged. `standard_failures` keeps the snapshot/string/checked-all/defer alternatives and cause privacy/automatic propagation. `fixtures` retains lexical templates, ordinal-root tables without argument checking, symbolic-root seams with checking, and defer; all describe callable creation receipts. `extensibility` keeps distribution admission, type-only arbitrary manifests, full-contract third-party packages and defer. `error_identity` preserves nominal identity versus global allocation/manual IDs/defer, and the nominal-primary option consistently permits explicit versioned **wire identifiers at serialization boundaries that require them**, without requiring textual wire names.

## Interpretation caveat, not a blocker

The packets now include two plausible structured normalization designs. Several other choice sets remain asymmetric because some options intentionally violate the supplied constraints or incur wider redesign costs: `alias_only` does not remove seven caller arms, `opaque_single` loses typed selective recovery, and nested runtime wrapper catches can recatch transformed failures. A three-call consensus should therefore be recorded as advice about these stated alternatives, not proof of the complete architecture. The requests themselves say this explicitly. After dispatch, save all request/response pairs and investigate disagreement before committing, as `AGENTS.md` requires.
