# Coordinator preflight reconciliation

This review was completed before the three fresh live calls. It covers the final exact request hashes recorded in `preflight-final.json`.

All three independently drafted presentations were checked against the same canonical problem ledger. Rotating peer reviews covered all eight topics, all 29 alternatives, shared constraints, technical identifiers, numeric facts, native mappings, and evidence limits. The coordinator read the resulting shared contexts, topic facts, questions, and candidate definitions, including the corrections described in the peer reports. The presentations preserve the original decision problem; `snapshot_scope` explicitly marks it as retrospective rather than describing later adopted rules as still unresolved. Earlier Jev answers and later engineering selections are absent from all requests.

The mechanical check passes for all 54 natural-language fields in each request. Every corresponding field differs across each pair and from the original experiment; no complete shared natural-language sentence of at least eight words remains. Semantic topic keys, option keys and ordering, exact APIs/code/numeric facts, and the eight-question contract are unchanged. Similarity metrics establish wording variation, not semantic equivalence; the manual reviews supply the latter judgment.

## Final coordinator amendment

Round 1's peer review recorded the then-current request hash `6fd8` (prefix); the coordinator subsequently narrowed its instruction to: “Resolve these technical choices autonomously instead of presenting the user with another questionnaire.” This preserves the canonical autonomous/no-new-questionnaire scope rather than expanding it to a ban on every possible user question. The final round 1 hash is recorded in the gate; the peer report is retained as a then-current review, not represented as covering unchanged bytes. No technical fact, option, or preference changed in this amendment. The coordinator rechecked this final wording against the ledger and the other presentations, then reran the mechanical gate.

Technical section references contain identifiers only where descriptive wording had otherwise survived. This removal does not change the decision problem. Round 2 and round 3 peer reviews cover their final request bytes. The three presentations remain substantively equivalent after the recorded corrections, with genuine full-payload paraphrasing and no unresolved preflight issue.

The separate `engineering-before-results.json` records the coordinator's reassessment of all eight topics before the fresh responses. A result disagreement must be inspected against facts and engineering evidence; no pooled voting with the superseded experiment, acceptance threshold, or retry-until-agreement rule is authorized.
