{
  "model": "jev-latest",
  "state": {
    "purpose": "This consultation concerns an agent-oriented language where reliable behavior, dependable edits and externally visible results come first, while whole-task token cost across a successful assignment is tracked afterwards as a subordinate metric. The dated 24 September 2026 graph of tasks T01 through T27 proposes future work; it asserts no shipped compiler or runtime change. No outside adopters exist, so preserving prior source forms, binary interfaces, emitted file shapes or recorded goldens is explicitly not required. Emitted TypeScript should delegate each matching operation to the corresponding native JavaScript or Bun primitive, introducing extra helpers solely where Can invariants or frozen values demand them. Each gate from 1 to 5 needs repeatable linked proof, and a trial run alone never earns a mechanism its place. Reach a technical selection from the supplied facts alone, with no further user question.",
    "task_graph": "The graph starts with T01, which locks the implementation baseline and test rig: approved Bun and compiler releases, throwaway build folders, central regression invocations, paired old-versus-new diagnostic fixtures, plus a registered set of construction, restructuring and repair prompts with reserved variants, concealed checks and fixed model/effort configurations per the evaluation protocol. Once T01 is done, workers may pursue T02, T03, T06, T07, T18 and T19 concurrently. Completion of T03 enables T04, T05 and T08; T05 together with T06 enables T10; T03, T05, T06 and T07 jointly enable T21. T10 enables T11, T12 and T13; T21 enables T22. T09 unifies T02 through T08 over the compiler, package graph, runtime metadata, formatter, public documentation, CLI diagnostics and samples, executes the whole compiler/runtime regression and cross-cutting negatives (an owner record crossing a generic and package edge, a variant leaf crossing an import, a fixture link surviving an alias rename, an error report surviving a lineage change), rebuilds generated outputs a single time from the unified source, and closes Gate 1/2 only on repeatable results; T09 gates T16, whereas the invoice and browser tracks advance under their own named prerequisites. T10 introduces source action declarations and verified catalogue symbols from the exact-path parsing and typing agreement, promoting the chosen captured routes to the public invoice surface with no legacy alias. T14 delivers the guarded invoice reads and writes plus three slim action handlers on a genuine throwaway database. T17 executes the full server product matrix for Gate 3; T19 fixes official Bun 1.4.2 Linux x64 onto Debian 13 amd64/glibc; T20 certifies Gate 4 server SaaS against that installed Linux build. T21 ships canlc build --target browser as its own main-thread root and profile with a transitive capability seal and a compiler-emitted content-hashed same-origin bundle. T26 refreshes documentation and runs reserved agent trials; T27 closes with release audit. Compiler or catalogue changes from separate workers must never be combined sight unseen; reconcile onto a single pinned revision and repeat impacted verification.",
    "evidence_limits": "Available measurements are narrow and throwaway. Eleven passing roots in a values/importer experiment demonstrated invalid inhabitants escaping through ordinary construction, copy replacement, structural decoding, fixture arguments and mocked completions despite factory refusal. Thirteen pattern trials revealed a last-arm decliend and an inner paidd quietly becoming binders, plus one spelling acting as a nominal check beneath payment and as a capture beneath int. Sixteen variant trials yielded twelve successful builds, twenty-four satisfied assertions and four anticipated static refusals, exposing a route-dependent direct block alongside bridge and shared-leaf acceptance. Twenty-eight of twenty-eight helper assertions held across two single-error caller domains. Twelve FormData checks proved bare parallel lists can attach one row's price to another row after omissions. Eighty-two route assertions on fixed Bun define guarded native dispatch with method-first and static-within-method order and exact malformed-escape/dot/encoded-separator handling. A browser trial watched an authentic 503 body arrive while outdated 422 guidance stayed on screen. Eighteen invoice assertions completed with no genuine store, wire reply or document model. No certified live invoice path, replay ledger, grid client, or Debian/Bun deployment exists. Reserved agent trials are unexecuted and token consequences unknown. Model scores advise but never certify; the present calls execute no generated program and measure no domain accuracy.",
    "fixed_constraints": "Each compiler or catalogue edit ships with passing-case coverage, failing-case coverage and span-precision checks for diagnostics, together with refreshed generated TypeScript and golden files wherever relevant. Any modification under authored runtime/ or tools/runtime/ TypeScript requires bun run lint:fix:runtime followed by bun run format:runtime, then bun run check:runtime plus the scoped test selection, with bun run lint:runtime --format=agent available for terse findings. The generated runtime/catalogue.ts and locked vendor blobs are off limits for manual revision. Product-facing verification has to examine genuine wire replies, rendered documents and stored rows rather than stopping at an attached Can check or a model recommendation. Every qualification write-up logs the concrete model identity and release, compiler commit, host details and observed faults. Postponed features remain excluded from construction: DI-03 finite error-set generics, DI-08 capture binders, DI-14a RETURNING on mutation, DI-17 fresh AI wrapper/batch/callable natives, DI-18 static escape or consumed-fault extensions, DI-19 open SDK imports, DI-20 batched immutable collections, DI-22 named-field construction and DI-23 multiline syntax. Implementers may not conjure them to satisfy a test."
  },
  "questions": {
    "integration_strategy": {
      "type": "choice",
      "instructions": "Select the unification approach for the T02 through T08 compiler and catalogue changes that best protects stable cross-feature behavior while respecting the stated lane dependencies. Treat token impact as unknown.",
      "criteria": {
        "single_locked_integration": "Unify the T02 to T08 edits on a single pinned tree spanning checker, emitted metadata, package identity, formatting, command output, manuals and samples. Execute the complete regression and intersectional denials for protected values at generic and package seams, variant leaves at import seams, scenario linkage across renames, and report keys across lineage edits. Rebuild derived artifacts exactly once from that tree and note deliberate breaking changes. Credit Gate 1/2 solely when reruns match; hold T16 behind T09 and let invoice and browser tracks follow their named gates.",
        "per_lane_incremental": "Permit every core track to integrate and refresh its snapshots on its own cadence, combining branches when convenient and deferring seam-spanning failure tests until product lanes surface a mismatch.",
        "block_all_on_core": "Halt all product and platform tracks pending complete T09 sign-off, refusing concurrent effort on invoice, browser, webhook or shutdown tasks regardless of their stated independent prerequisites."
      }
    },
    "replay_ledger": {
      "type": "choice",
      "instructions": "Pick the invoice replay and denial policy that best handles uncertain commits, cross-tenant reads and typed save outcomes across both adapters. Judge idempotent effects and honest failure signals.",
      "criteria": {
        "same_transaction_ledger": "Persist the replay record, binding authenticated principal, invoice key, client operation identifier, content hash and acknowledged reply, atomically alongside the guarded row change. Repeating the same identifier with matching bytes replays the stored outcome without duplicating the business effect, while the same identifier with altered bytes reports a clash. Unknown and foreign invoice keys receive an identical opaque 403 once identity is known; corrupt path encodings are rejected 400 ahead of guarded logic. Expose five HTML save results, five JSON save results and three JSON load results with typed shapes, honoring an 8192-byte JSON body ceiling and 2048-byte/64-row HTML form ceilings. After ambiguity the form rereads before writing again, and the grid preserves its tagged attempt for a byte-identical resend or a permitted snapshot fetch. A limited retention window sustains later reconciliation; a render fault following durable commit can present 500 with unknown effects.",
        "separate_ledger_transaction": "Apply the guarded row update in one transaction and file the idempotency and dispatch rows afterwards in another commit, reconciling gaps asynchronously and tolerating a span with effects but no replay key.",
        "client_new_id_retry": "On uncertain endings including timeouts, disconnects, cancellations and view teardowns, generate a new client token per attempt and let the backend apply each token as an independent write, risking repeated effects."
      }
    },
    "browser_closure": {
      "type": "choice",
      "instructions": "Choose the compilation and packaging rule for canlc build --target browser that best isolates the grid client from server capabilities while keeping wire parity checkable.",
      "criteria": {
        "strict_transitive_closure": "Compile the browser program through its own main-thread entry and target profile, verifying every reachable declaration, specialization and callback for browser fitness. Bar database handles, operating-system processes, filesystem paths, hidden credentials and server-side cryptography even via wrappers or generics, then inspect the output graph and maps for leaks. Reuse the pinned pure contract libraries and identical strict serialization on both runtimes, proving integer, float, variant, optional, extra-field and protected-record edges across declared browser versions. Expose only narrow opaque handles for application, view, node and state with guarded construction, text-centric rendering, snapshotted events, owner-tied cleanup and compare-and-publish state. Ship solely a compiler-built content-hashed same-origin module; forbid hand-written scripts and background-thread profiles. Treat network cancellation as client-side abandonment without rollback meaning.",
        "runtime_guarded_shared": "Keep a unified compilation pipeline for both runtimes, ship server-reaching code inside the client bundle, and block forbidden operations dynamically if the browser path happens to invoke them.",
        "permissive_bundling": "Open the browser target to hand-written modules, embedded script tags, external client libraries and threaded execution next to generated code, dropping the single-artifact hash and origin limits plus the import review."
      }
    }
  }
}
