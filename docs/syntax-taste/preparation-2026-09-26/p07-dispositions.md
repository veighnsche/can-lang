# P07.1 disposition ledger — every finding resolved

Dispositions: CHANGE / RETAIN / SATISFIED / REJECT / DEFER. Reasons cite
evidence; deferrals carry reopening conditions + scope effects. Full-breadth
scope (S-ambition) + widest matrix (S-platforms) + companion worker (S-worker)
govern below.

## R01 host integration

| ID | Disp | Reason |
|---|---|---|
| F-R01-01 | CHANGE | Closed catalogue gains a decided integration path (not a bigger closed wall): X-R01-1 discriminates adapter vs companion per widget class; likely split (small native ops → adapters, large SDKs → companion) is a hypothesis the experiment confirms or refutes. |
| F-R01-02 | CHANGE | Same experiment; Jev 3/3 reviewed_adapters (weakening 0.71→0.48) is advice only — catalogue growth stays live via the C3 near-tie. |
| F-R01-03 | CHANGE | W2 acceptance: second vendor without vendor-specific compiler patch or hand-edited code (P09-M1: generic reviewed-path catalogue additions permitted if X-R01-1 selects catalogue-only for the class). |
| F-R01-04 | CHANGE | P11 reopened by full-breadth scope: the two-concrete-ops comparison runs. |
| F-R01-05 | RETAIN | Fixed-origin + confined paths stay until R11 destination policy says otherwise; companion home keeps them. |

## R02 browser controls/UI

| ID | Disp | Reason |
|---|---|---|
| F-R02-01/02 | CHANGE | Minimal-sufficient native addition set via X-R02-1 (each addition: workflow + bounded projection + Chromium/WebKit/Firefox tests). |
| F-R02-03/05 | CHANGE | Library extraction + second-app consumption (W1); Jev library 3/3 (0.97/0.91/0.89). |
| F-R02-04 | RETAIN* | Append contract stays unless X-R02-1 demonstrates an inexpressible nested shape (*conditional change, precisely gated). |
| F-R02-06 | CHANGE | P08/P12 reopened by scope; P10 history/WS stays deferred (no review workflow needs it — prior conditions stand). The R16-04 story states the rich-client boundary explicitly (P09-M5). |
| F-R02-07 | RETAIN | Grid strengths are preservation criteria, tested by existing legs. |

## R03 captured routes

| ID | Disp | Reason |
|---|---|---|
| F-R03-01/02 | CHANGE | O1 selected: captured GET actions may declare `body html` with a `document` response mode for full-page reads vs `swap inner` fragments; reuses capture/URL/ambiguity machinery; no SPA router (Q7). |

## R04 budgets/cancellation

| ID | Disp | Reason |
|---|---|---|
| F-R04-01..05 | CHANGE | Written request-policy spec + O1 operation deadlines first (Jev 3/3, 0.66–0.74), O2 scope policy evaluated second; SQL/browser-action operands after native qualification; shutdown escalation documented; W5 acceptance. |
| F-R04-06 | CHANGE | P06 reopened. P07 empty-race semantics: RETAIN prior (review asks nothing). |

## R05 iteration/collections

| ID | Disp | Reason |
|---|---|---|
| F-R05-01/02/04/05 | CHANGE | O2 self-tail lowering first (identical observable behavior); O3 explicit primitive only if the proof boundary excludes needed shapes (Q6). W4 acceptance with measured stack/memory. P05 reopened. |
| F-R05-03 | CHANGE | O-B2 native build + immutable publication with defined collisions/order/errors/ownership. P18 reopened. |

## R06 failure composition

| ID | Disp | Reason |
|---|---|---|
| F-R06-01/03/04/05 | CHANGE | X-R06-1 result-data experiment with oracle-verified attempts/sequences; finite error-set syntax only if adapters stay extensive (Q5). P04 reopened under that gate. |
| F-R06-02 | RETAIN | The 5-root pass stands with its narrowed meaning (representation, not retry proof). |

## R07 assertion setup

| ID | Disp | Reason |
|---|---|---|
| F-R07-01/03 | CHANGE | X-R07-1 factory-pattern extraction example + measured comparison; setup-region syntax only on demonstrated excess cost with explicit LD29 reopening (Q4). |
| F-R07-02 | RETAIN | Private fixture helper is the valid current convention; document it. |
| F-R07-04 | RETAIN | LD29 gate stays closed unless the Q4 condition triggers. |

## R08 authoring policies

| ID | Disp | Reason |
|---|---|---|
| F-R08-01 | CHANGE | Q1: allow both Boolean orders, formatter canonicalizes to false-first. Reverses the 2026-09-23 user decision + P15 Retain (explicitly, by the same authority). |
| F-R08-02 | CHANGE | Q2: C8 error becomes advisory lint; formatter keeps source. Revises C8/DI-23 + P14 (explicitly reopened). |
| F-R08-03 | CHANGE | Q3: explicit `with` site bindings + fallback + safe rename. Revises LD38/LD39 + DI-08 + P13 (explicitly reopened). |
| F-R08-04 | SATISFIED | All three prior rows now have explicit user revisions above. |

## R09 editor

| ID | Disp | Reason |
|---|---|---|
| F-R09-01/02/03 | CHANGE | Full LSP sequence format → hover → references → completion → rename (Q8); rename follows the Q3 packet. User blocker answer 1A includes body-local references and safe rename by scope identity. |

## R10 relational

| ID | Disp | Reason |
|---|---|---|
| F-R10-01/05 | CHANGE | P20 reopened: RETURNING admitted only after a real post-write-read/race-cost demo + specified per-dialect semantics. Descriptor admission otherwise retained. |
| F-R10-02 | CHANGE | Blessed-encoding guide first; new column kinds/codecs only where encodings prove insufficient. |
| F-R10-03 | CHANGE | Real PG app acceptance + (widest matrix) MySQL live parity legs. |
| F-R10-04 | CHANGE | Lookup-first PG conflict recipe now (docs + example port); savepoint syntax only if the PG app proves need. |
| P19 | DEFER | Migration provenance stays deferred (no versioned-migration requirement in scope); effect: operator-owned DDL remains the boundary, stated in docs. Reopen: a versioned migration + drift case. |

## R11 workers/outbound

| ID | Disp | Reason |
|---|---|---|
| F-R11-01/02 | CHANGE | Companion architecture: documented protocol + auth envelope + Can/companion pair qualification at full ops scope (bounded concurrency, retries/backoff, poison, crash recovery owned by the companion; ledger/outbox/idempotency owned by Can). |
| Can-worker option | REJECT | For this round: S-worker chose companion. Reopen: a Can-worker prototype proving bounded multi-worker claims + crash recovery with lower total cost than the pair. |
| F-R11-03 | CHANGE | Destination policy for tenant-selected webhooks at the companion boundary (allowlist/pattern, credential binding, redirects, assertion story). Unrestricted fetch stays rejected under DI-19. |
| F-R11-04 | RETAIN | O04/O05 Can-side envelope (fixed-origin, trusted-carrier scope) hardened with the R16-03 auth bullet. |

## R12 observation

| ID | Disp | Reason |
|---|---|---|
| F-R12-01/02 | CHANGE | O2 builtin_auto redacted reporter (Jev 3/3, 0.80–0.96) with correlation/source identity, reusing main/late-owner/browser machinery. |
| F-R12-03 | CHANGE | Documented manual policy for expected failures; the default covers the rest. |

## R13 deployment

| ID | Disp | Reason |
|---|---|---|
| F-R13-01a | SATISFIED | Linux target implementation exists (Debian 13+ amd64, packaging, smoke). |
| F-R13-01b | CHANGE | Release qualification via UP25 on x86 (never on this Air). |
| F-R13-02 | CHANGE | Paired-deploy qualification + lifecycle recipe (service unit, health, rollout/rollback, credential provisioning). Migration story stays operator-owned per P19 deferral. |
| F-R13-03 | CHANGE | Old-browser-vs-new-server acceptance scenario defined + qualified. |
| F-R13-04 | RETAIN | Machine constraint honored in all lanes. |

## R14 AI qualification

| ID | Disp | Reason |
|---|---|---|
| F-R14-01/02 | CHANGE | User 2A/3A select tenant token allowance with immediate typed rejection. The [follow-up contract](blocker-resolution/README.md) fixes combined counting, configured fixed epochs, durable native reservations, conservative unknown usage and qualification before dispatch; support-ticket-triage model-change evaluation remains live W6 evidence. No money product cap or queue. |
| F-R14-03 | RETAIN | Narrow non-streaming adapter stays unless an accepted requirement expands it. |

## R15 S3 (defects — must close one way)

| ID | Disp | Reason |
|---|---|---|
| F-R15-01/02 | CHANGE | O1 fix-to-catalogue if `end(Error)` verifies as a no-complete release; else honest destructive re-spec with caller-visible warnings (O2). A silent delete under "cancel" is rejected under all outcomes. Isolated qual (a)/(b)/(e)/(f). |
| F-R15-03/04 | CHANGE | Effective deadline (O1) if a bounded-abort design verifies; else honestly documented between-awaits bound (O2) — but O2 fails W5 and needs explicit user scoping if taken. Isolated qual (c)/(d). |

## R16 docs/examples

| ID | Disp | Reason |
|---|---|---|
| F-R16-01 | CHANGE | Bare `env::invalid_name("")` + comment rewrite. Lane C. |
| F-R16-02 | CHANGE | Linux reword ×2 ("implemented; qualification deferred (UP25)"). Lane H. |
| F-R16-03 | CHANGE | Webhook limits bullet rewritten to describe the AUTHENTICATED carrier envelope (loopback/trusted + auth design from Lane F) — the "unauthenticated" reading is superseded by the R11 auth contract (P09-B9). Lane F, conditioned on the auth design. |
| F-R16-04 | CHANGE | One supported story written after R10–R14 boundaries land. |

## Prior rows untouched by this review (standing confirmed, not re-decided)

RETAIN/DEFER as dispositioned on 2026-09-24: P07 race-empty semantics, P09
reload drafts, P10 history/WebSocket, P16 multiline delimiters, P17 cleanup
helpers, P21 owner equality, O01 batch atomicity, O02 choice cardinality, O03
native recovery/grouped callables, P01/P02 (absorbed into the R01 decision).
P13/P14/P15 revised above by explicit user authority; P03 delivered; all other
P-rows reopened as recorded.
