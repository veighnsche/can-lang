# Core design resolution status

**Status after the upgrade (24 September 2026):** Historical preparation-stage working record. Later selections and T01–T27 execution supersede its pending-work status. The post-upgrade reconciliation records which core requirements remain incomplete. See the [current reconciliation](../post-upgrade-reconciliation-2026-09-24.md).

24 September 2026 · working record. This is not yet the authoritative language
specification or an implementation task list. The [decision inventory](decision-inventory.md)
remains the coverage source, and current behavior in `decisions.md` remains
authoritative until an accepted choice is integrated.

| Topic | Current engineering position | What must close before specification |
| --- | --- | --- |
| DI-01 pattern intent | Favor checked bare nominal tests and explicit captures uniformly at all ordinary-data pattern depths. The user selected `bind name` for whole and nested captures ([confirmed scope](confirmed-syntax-choices.md#ordinary-data-pattern-capture--di-01)). The [nested probe](pattern-nested-probe.md) shows silent nested typos; [Jev](jev-pattern/findings.md) split at low confidence. | Check interaction with chosen variant representation, generic specialization, tail `...rest`, diagnostics and held-out edit cases. |
| DI-02 validated values | [Selected `owner record`](owner-record-design.md), keeping transparent DTO records. The [two-package probe](validated-value-probe.md) verified 11 assertions and demonstrated importer constructor, `with`, JSON decode and fixture bypasses of factory predicates. Three further [fresh Jev requests](jev-owner-record/findings.md) informed the full contract. | Integrate canonical package-instance authority with DI-05a, generic specialization and fixture transport; execute planned acceptance matrix during implementation. |
| DI-03 finite failures | The [28-assertion probe](generic-helper-probe.md) shows fixed bounds add impossible caller arms and generic result data needs adapters. [Jev](jev-generic/findings.md) advised a finite error-set trial, not adoption; [concrete trial design](finite-error-design.md) is ready. | Defer production syntax until a registered held-out creation/refactor/repair comparison shows a material benefit over current chains, fixed bounds and result-as-data. |
| DI-04 generic operations | [Selected planned design](accepted-technical-decisions.md#di-04--explicit-operation-inputs-for-exported-generics): exported bodies are checked under symbolic types and written callable/dictionary inputs; local templates retain specialization checking. The current probe demonstrated explicit inputs; [fresh Jev](jev-generic-offline/findings.md) favored this contract. | Specify diagnostics for declaration-time hidden operation and caller repair; implement/check the symbolic typing rule and native callable lowering. |
| DI-05a packages | The user selected dependency-qualified source imports with file-local aliases ([confirmed scope](confirmed-syntax-choices.md#dependency-qualified-source-imports--di-05a)). The [complete contract](package-error-contract.md) selects owner-declared project/instance lineage UUIDs and a canonical graph lock; global short-name lookup is replaced. | Production tests must exercise direct/transitive visibility, duplicate paths, divergent same-lineage rejection, locks, private packages and relocated checkout. |
| DI-05b errors | [Selected for planned design](accepted-technical-decisions.md#di-05b--unnumbered-application-errors-with-qualified-report-identity): unnumbered declarations and qualified textual identity. The [complete contract](package-error-contract.md) specifies `can.error.v2` keys, report schema, lineage-scoped retirement and two-build checks; the [consumer audit](error-identity-audit.md) found no naked stable application-number requirement. | Coordinate all loader/checker/runtime/CLI consumers, graph locks, concrete generic identity and redaction; execute the two-build matrix. |
| DI-06 fixtures | [Selected planned design](fixture-scenario-design.md): same-owner lexical rows plus checked exported helper scenarios. The [hard-case probe](fixture-hard-case.md) measured ordinary test-entry API cost and a disposable scenario runtime path; [fresh post-spike Jev](jev-fixture-post-spike/findings.md) favored F2 with residual support for test entry. | Production parser/checker/runtime must prove static links, concurrent FIFO isolation and evidence labels; a disposable generated-output spike is not conformance. |
| DI-07 variants | [Selected for the planned design](accepted-technical-decisions.md#di-07--extensional-closed-variant-compatibility): extensional inclusion of fully specialized nominal leaf sets, retaining generic-record identity. The [16-case baseline matrix](variant-matrix-probe.md) is complete. | Integrate exact assignment/codec/diagnostic rules in P8; production tests include nested/leaf-addition cases. Reopen only for a demonstrated variant-brand requirement. No new syntax. |
| DI-08 captures | Use explicit context records in the product flow before changing `near`. Current exact-name capture can redirect under a same-typed shadow. | Run parameter-rename/same-typed-shadow task on the invoice slice; only then consider an explicit capture map and Jev-advised syntax resolution. |

These positions follow the [core alternatives](core-alternatives.md) and
[three fresh core Jev requests](jev-core-contracts/findings.md). A task-level
token advantage is not claimed. A retained or deferred disposition still needs
its reason and reopening condition when P8 integrates the whole design.
