# Typed browser action consultation

24 September 2026. Three fresh SystemOne requests were reviewed for semantic equivalence before sending; [the wording audit](wording-audit.json), complete [requests](request-1.json), [second request](request-2.json), [third request](request-3.json), and corresponding [responses](response-1.json), [second response](response-2.json), [third response](response-3.json) are saved here. All explanatory prose was rewritten; facts and alternatives stayed fixed.

Jev selected `separate_json_action` in all three wordings, with probabilities .92, 1.00 and .94. The closest alternative was a negotiated dual-format action at .07, .00 and .05. This consistency is advice, not implementation or agent-task evidence. Engineering selects two source actions because the HTML action's `form::rows` is deliberately not JSON-admissible and its safe HTML fragment is not a typed browser result. A dual-format declaration would couple separate decoding, rendering and client policies in the first grammar. Status-only HTML consumption would require an additional typed read or DOM parsing. Both actions will share one protected server operation, with authorization, revision and mutation identity checked there.
