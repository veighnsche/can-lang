# Product and platform resolution status

24 September 2026 · working engineering record, not authoritative syntax or a
production implementation. The [current invoice slice](invoice-current-probe.md)
passes 18 Can assertions but has no live SQL/HTTP/browser observation. The
[product alternatives](product-alternatives.md), [three fresh Jev rounds](jev-product-contracts/README.md)
and [actual HTMX 503 browser probe](htmx-503-probe.md) support the trial order
below; Jev's agreement is advice, not proof.

| Topic | Working contract or strongest baseline | Required closure before specification |
| --- | --- | --- |
| DI-09a endpoint | [Selected for planned design](accepted-technical-decisions.md#di-09abd--source-action-contract-for-exact-path-forms): one source `action` declaration links exact mount, method, URL, shallow wire form and finite response cases. [Fresh Jev consultations](jev-remaining-syntax/findings.md#di-09--choose-a-source-action-declaration-with-bounded-semantics) favored source placement. The separately [specified capture increment](route-capture-design.md) follows 82 guarded native-route assertions and 27 live HTTP cases under pinned Bun 1.4.2. | Implement/check the exact parser/catalogue signatures, route/field/method renames, same-name imports, unknown route references, strict capture failures, native 400/404/405 fallback and lifecycle integration. A safe URL implies neither authorization nor route deployment. |
| DI-09b form | [Select checked keyed rows](keyed-row-design.md) as the bounded extension to shallow string-oriented form data; the [12-check native probe](keyed-row-probe.md) reproduced equal-length parallel-array misassociation and keyed rejection/retention. Direct field references carry wire owner/type; domain values still enter through owner factories. | Implement precise row-key/order/member decoder, retained pre-handler 422 raw view, field/problem references and independent wire/domain rename diagnostics; verify real form/DOM/database behavior. |
| DI-09c authorization | Keep explicit server-controlled actor/context and check membership plus resource/tenant/revision at the protected read/write. A valid tenant ID is not authorization. | Real-database cross-tenant, concurrent membership/revision and uncertain-commit cases; choose disclosure policy for missing vs forbidden; evaluate capture context baseline before a scoped capability. |
| DI-09d action/target | The [Chromium probe](htmx-503-probe.md) observed 200 and 422 fragments swapping, while an actual 503 response delivered feedback HTML but left stale 422 content in `#account_results`; the pinned HTMX config excludes 503 from swaps. Make 200/422/409/403/503 status and render/swap behavior explicit. A target reference can be checked only within a stated render scope; conditional absence remains runtime. | Choose a checked response/swap policy using actual DOM evidence. Test retained fields/focus, target removal and out-of-order responses; preserve real HTTP status. |
| DI-10 HTML | Reuse escaped safe builders, named render helpers and native Bun escaping. Trial static validation for literal builder calls before new declarative grammar. | Hostile text/attribute and dynamic-structure tests, builder/formatter agent edits, accessibility tags required by the real grid. No markup syntax question yet. |
| DI-11 browser | [Selected explicit native-DOM browser target](browser-target-design.md), with ordinary Can functions, immutable snapshots, a small browser catalogue and app-owned identified saves separate from view listeners/timers. The [offline contract](jev-generic-offline/findings.md) keeps an in-view draft and explicit retry/reconciliation, without reload durability or automatic queue. | Same-grid browser/version tests for state, exact numbers/codecs, lifecycle, offline/reconnect, accessibility and capability rejection. Fault-test durable draft/queue separately before any later durability promise. |
| DI-12 sequencing/cleanup | Use `match chain`, named helpers and current owner scope; repair the stream example's explicit-close promise or wording. | Inject read and close failures to define primary/secondary reporting and exactly-once closure. No general `finally` syntax selected. |
| DI-13 assertions | Keep attached assertions and lexical fixtures; use companion protocol/browser/database scenarios for actual status, DOM and durable effects. Jev split on a source-authored runner. | Run wrong-body 200, unswapped 503, cross-tenant and duplicate-webhook negatives; adopt authored orchestration only after a measured reuse/repair gap. |
| DI-14 SQL | State current statement/parameter/cardinality checks plus runtime row validation accurately. `RETURNING` is a bounded trial only after a real canonical post-write case; schema snapshot is a separate provenance-sensitive optional trial. | Live transaction/read-after-write baseline, mutation cardinality and zero-row semantics, schema-drift check, uncertain commit lookup. Do not claim snapshot equals deployment. |
| DI-15 lifetime/races | Preserve native winner/empty semantics and runtime owner leases. Inventory abort support per operation and qualify pending losers/shutdown under a host deadline. | Real service fault and deadline matrix; optional consumed-fault observation only if signal beats noise. A client abort does not roll back a commit. |
| DI-16 Linux | The first target is [Debian 13 amd64/glibc with official Bun 1.4.2 Linux x64](linux-target.md). Current release qualification remains Darwin ARM64 only. | Pin archive/image bytes, build/verify Linux launcher and isolation, then run native, invoice, webhook and shutdown cases on the installed artifact. |

This table records a defensible route to design closure; it does not treat an
unrun product qualification as already passed. End-to-end qualification can be
an implementation task once its contracts and acceptance observations are
fully specified. Any unresolved syntax or architecture choice must be settled
before P10 ordering.
