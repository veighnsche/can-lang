# Supporting-topic resolution status

**Status after the upgrade (24 September 2026):** Historical preparation-stage working record. Later selections and T01–T27 execution supersede its pending-work status. The post-upgrade reconciliation records current support boundaries and proposals. See the [current reconciliation](../post-upgrade-reconciliation-2026-09-24.md).

24 September 2026 · working disposition draft. The
[support alternatives](support-alternatives.md), [source evidence](ai-support-evidence.md)
and [three fresh Jev consultations per set](jev-support-contracts/findings.md)
do not establish new syntax or measured agent benefit. Current `decisions.md`
rules remain authoritative pending P8 integration.

| Topic | Draft disposition for the implementation-plan scope | Reopening evidence or remaining work |
| --- | --- | --- |
| DI-17a LLM wrapper provenance | Retain ordinary named LLM wrappers and current fetch/judge `wrap` scope. | A real same-kind authored/decoder recovery case that ordinary code cannot handle without replay or origin loss; then trial a narrow provenance-aware LLM wrapper. |
| DI-17b judge batch effects | Retain ordered handler effects without rollback and document the prepare/validate/handle/result phases. | Ledger flow requiring atomic handler effects; use explicit transaction/outbox trial rather than assuming batch is a transaction. |
| DI-17c grouped callables | Retain named wrappers for grouped judge/LLM state. | A fan-out task with measured adapter error or repair cost; then consider direct reference semantics with complete state grouping. |
| DI-18a scoped result | Retain runtime owner guard and pre-native `resource_state` rejection. | A narrow static obvious-escape diagnostic must avoid false positives on enclosing live resources and survive alias/capture cases; improve runtime context first where agent repair fails. |
| DI-18b race faults | Retain native first-success selection and current diagnostics. | Fallback service where consumed fault visibility has useful signal without routine noise; compare application observer before new race policy. |
| DI-19 controlled catalogue | Retain typed HTTP/codec baseline and closed platform admission. | A named provider/SDK operation materially blocked or unsafe under current primitives; compare a distribution adapter before any third-party manifest. |
| DI-20 immutable bulk | Retain native immutable map/set semantics. | Measured unique-key volume/throughput and task-level agent cost show current clone-per-insert fold is material; then trial targeted native bulk operation. |
| DI-21 billing/calendar | Build a policy-explicit domain library/example using current exact minor-unit and zoned-time primitives. | Executable edge case proves a missing exact native primitive; add only that catalogue operation. No billing grammar. |
| DI-22 named construction | Retain positional constructors provisionally; compare domain-type/factory baseline with a named-field trial on same-type reorder/add/rename. | Silent agent misbinding or material repair benefit after controlled edit; if a form is adopted, resolve syntax and field-order/evaluation rules with fresh Jev advice and engineering judgment. |
| DI-23a multiline | Complete already accepted formatter, comment preservation and precise diagnostic spans first. | Held-out agent edits still fail materially on long delimited forms; then evaluate continuation syntax with Jev advice. Human line width alone is insufficient. |
| DI-23b unnecessary local | Retain current validity until a meaningful-local versus accidental-alias agent comparison. | If rejection adds repair cost without preventing semantic harm, make style advisory with safe fix; preserve exact evaluation count. No new grammar needed. |

The table explicitly defers mechanisms with a stated reopening condition. The
final P8 disposition may promote, retain or reject a mechanism if the bounded
probes or cross-topic contracts change the evidence. It must not convert an
unmeasured source-length complaint into an implementation task by default.
