package gallery
    provides []
    uses []

/// A function can return text; its assertion is a runnable example.
fn str hello
    emits []
    asserts
        greeting: => ok "Hello, world!"
    ok "Hello, world!"
